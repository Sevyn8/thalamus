# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Stray 0-byte artifacts pnpm writes to the cwd in this environment (named after
# the package + the binary). Anchored to the dir root so they never match
# anything under node_modules.
/dis-ui-ver2@0.0.0
/vite

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
