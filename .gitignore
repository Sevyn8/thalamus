.venv/
node_modules/
__pycache__/
*.pyc
.env
.env.*
!.env.example
.terraform/
*.tfstate
*.tfstate.*
*.bak
# real terraform var values (never commit; only *.example is tracked)
terraform.tfvars
*.auto.tfvars
crash.log
crash.*.log
dist/
build/
.next/
.turbo/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.import_linter_cache/
cloud-sql-proxy
target/
.DS_Store
*.log
*:Zone.Identifier

# transient terraform import staging scaffolding (not source)
infra/_import/
*.tfplan
tfplan

# Working notes, specs and throwaway harnesses. Not build output — DESIGN INPUT
# that has already been acted on, so committing it would leave a second,
# unmaintained copy of decisions whose real home is the code and the docstrings.
# The UI build spec that produced slice 8a lived here; three of its capability
# rows were wrong, and a committed copy would outlive the correction.
scratchpad/

# Claude Code worktrees (local scratch, never committed)
.claude/worktrees/
