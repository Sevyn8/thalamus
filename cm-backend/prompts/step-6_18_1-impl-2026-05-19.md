# Step 6.18.1 : Verification + remaining edits + commit (CC execution)

**Date:** 2026-05-19
**Context.** Operator manually completed:
- Excel edit (data/ithina_dev_seed_data.xlsx): +1 permission row (p40, ADMIN.ROLES.OVERRIDE.GLOBAL), +1 role_permissions row (r_super_admin -> p40).
- Local seed: `uv run python -m scripts.seed_dev_data --reset` ran clean.
- Cloud SQL: inline UPSERT applied via Cloud SQL Studio (used `core.uuidv7()` schema-qualified; created_by_user_id = anjali@ithina.ai's id since bootstrap@ithina.ai does not exist in cloud).
- Verified both envs: permissions=36, role_permissions=132, SUPER_ADMIN holds the new permission.

CC's job: make the remaining text-file edits, verify, report, pause for authorisation, commit.

## Standing discipline

- A8 cite-or-verify: every codebase claim cites file:line.
- A7 commit gate: report before staging. STOP. Wait for operator authorisation before committing.
- A9 pre-flight gate: produce per-check pre-flight report. STOP. Wait for authorisation before edits.
- Contradiction-surfacing license: if reality diverges from this prompt, surface in report.
- No em-dashes in new prose.
- Do NOT push.

## Pre-flight (A9)

```
Check #1: prior commit + working tree
  Command: git log -1 --oneline && git status --short
  Expected: HEAD = 839cabf (Step 6.17.4) or later; tree clean or only
            this prompt's untracked file present.
  Output: <transcript>
  Status: matches / deviates

Check #2: local DB catalogue verification
  Command: psql "${DATABASE_URL/postgresql+psycopg/postgresql}" -c "
    SELECT 'permissions_total' AS check_name, COUNT(*)::text AS value FROM core.permissions
    UNION ALL
    SELECT 'role_permissions_total', COUNT(*)::text FROM core.role_permissions
    UNION ALL
    SELECT 'new_perm_row', code FROM core.permissions WHERE code = 'ADMIN.ROLES.OVERRIDE.GLOBAL'
    UNION ALL
    SELECT 'super_admin_holds_new_perm', COUNT(*)::text
      FROM core.role_permissions rp
      JOIN core.roles r ON r.id = rp.role_id
      JOIN core.permissions p ON p.id = rp.permission_id
      WHERE r.code = 'SUPER_ADMIN' AND p.code = 'ADMIN.ROLES.OVERRIDE.GLOBAL';"
  Expected: permissions_total=36; role_permissions_total=132;
            new_perm_row=ADMIN.ROLES.OVERRIDE.GLOBAL;
            super_admin_holds_new_perm=1
  Output: <transcript>
  Status: matches / deviates

Check #3: Excel state
  Command: python3 -c "
    import openpyxl
    wb = openpyxl.load_workbook('data/ithina_dev_seed_data.xlsx', read_only=True)
    perms = wb['permissions']
    rp = wb['role_permissions']
    print('permissions rows:', sum(1 for _ in perms.iter_rows(min_row=2)) )
    print('role_permissions rows:', sum(1 for _ in rp.iter_rows(min_row=2)))
    # find ADMIN.ROLES.OVERRIDE.GLOBAL
    for row in perms.iter_rows(values_only=True):
        if row[6] == 'ADMIN.ROLES.OVERRIDE.GLOBAL':
            print('found in permissions sheet:', row)"
  Expected: permissions=40 rows, role_permissions=137 rows (per the
            local Excel content; counts assumed from cited prior state
            + this step's +1 each. Adjust if actual differs).
  Output: <transcript>
  Status: matches / deviates

Check #4: baseline test count + seed_loader test
  Command: uv run pytest tests/integration/test_seed_loader.py -v --tb=short 2>&1 | tail -30
  Expected: this will likely FAIL until EXPECTED_VISIBLE_COUNTS_PLATFORM
            is updated for the new counts (35->36 and 131->132). Report
            the actual failure verbatim if so.
  Output: <transcript>
  Status: PASS / FAIL with the count-mismatch we expect

Check #5: find EXPECTED_VISIBLE_COUNTS_PLATFORM location
  Command: grep -n "EXPECTED_VISIBLE_COUNTS_PLATFORM" tests/integration/test_seed_loader.py
  Output: <transcript with line numbers>
  Status: located / not found

Summary: N/5 checks match; K deviations; J ambiguous.

STOP. Awaiting operator authorisation before edits.
```

## Work buckets (after pre-flight authorisation)

### Bucket 1: tests/integration/test_seed_loader.py MODIFY

Update `EXPECTED_VISIBLE_COUNTS_PLATFORM`:
- `permissions`: 35 -> 36
- `role_permissions`: 131 -> 132

If pre-flight Check #4 revealed different baseline numbers, use the actual numbers (current count + 1). Surface the divergence.

Verify after edit:
```
uv run pytest tests/integration/test_seed_loader.py -v --tb=short
```
Expect: all tests pass.

### Bucket 2: BUILD_PLAN.md MODIFY

Add a new Step 6.18 section after the Step 6.17 block. Verbatim text:

```markdown
### Step 6.18 - Role edit (PATCH /api/v1/roles/{role_id})

**Status.** TODO. **Owner.** CLAUDE_CODE (impl) + HUMAN (Excel + Cloud SQL).
**Blocked by.** None.

**Goal.** Ship the role-edit feature: SUPER_ADMIN can edit a role's name,
description, and permission set via PATCH /api/v1/roles/{role_id}. Also
adds GET /api/v1/roles/{role_id} (detail) for the edit screen UX.

**RBAC posture.** PATCH gated by ADMIN.ROLES.OVERRIDE.GLOBAL (new
permission introduced in 6.18.1). GET stays GATE_EXEMPT per FN-AB-30
deferral. Only SUPER_ADMIN holds OVERRIDE.GLOBAL initially.

**Sub-steps.** 6.18.1 (catalogue seed) -> 6.18.2 (GET detail) -> 6.18.3 (PATCH).

------

### Step 6.18.1 - Catalogue seed delta (ADMIN.ROLES.OVERRIDE.GLOBAL)

**Status.** DONE-LOCAL + CLOUD SQL APPLIED (2026-05-19).
**Owner.** HUMAN (Excel + Cloud SQL inline UPSERT) + CHAT (SQL drafting).
**Blocks.** 6.18.2.

**Goal.** Add ADMIN.ROLES.OVERRIDE.GLOBAL permission and grant to SUPER_ADMIN.

**Scope in.** Excel +1 permission row (p40); +1 role_permissions row
(r_super_admin holds p40); test count updates (permissions 35 -> 36,
role_permissions 131 -> 132). Cloud SQL inline UPSERT applied via Cloud
SQL Studio (operator-driven; uses anjali@ithina.ai as the
created_by_user_id since bootstrap@ithina.ai does not exist in cloud).

**Scope out.** No application code.

**Acceptance.** Local + Cloud SQL hold the new permission and grant.
Test suite green.
```

Note for placement: the existing Step 6.17 entries end with 6.17.4. Insert the new Step 6.18 block immediately after, before any Step 7.x entry. Match the surrounding section divider style (lines of `-` characters).

### Bucket 3: CLAUDE.md MODIFY

Two additions:

**3a. One-line pointer to step doc** (find the "Current state" or "Completed steps" section; mirror Step 6.17.4 entry's location/style):

```markdown
See `docs/implementation-steps/step-6_18_1-roles-catalogue-2026-05-19.md` for plan, mental model, and retro.
```

**3b. Two new FN-AB entries** (find the FN-AB section; append at the next available FN-AB-NN number):

```markdown
### FN-AB-NN : Cross-env audit-actor drift on catalogue seed inserts

Step 6.18.1's local seed used Excel `created_by_user_id` referencing
bootstrap@ithina.ai (a user that exists in local seed). Cloud SQL has
no bootstrap@ithina.ai user; operator's inline UPSERT used
anjali@ithina.ai's id instead. The `core.role_permissions.created_by_user_id`
for the new SUPER_ADMIN -> ADMIN.ROLES.OVERRIDE.GLOBAL row differs
between local and cloud.

Not load-bearing: both rows satisfy ck_role_permissions_created_by_actor_pair
(non-null + matching user_type). Audit history is divergent across envs
for this single row.

Resolution criterion: either (a) seed Excel converges on a user that
exists in all envs (anjali@ithina.ai is the obvious candidate), or
(b) the catalogue-delta workflow standardises a "system actor"
convention for operator-driven inserts. Revisit at next
catalogue-delta sub-step or when audit_log integration (Step 6.2)
makes this observable.

### FN-AB-NN : NIGHT_SHIFT_LEAD is_system=false intent

Investigation for Step 6.18 surfaced NIGHT_SHIFT_LEAD as the sole role
with is_system=false in the seed. Intent unclear: test data, custom-
role demo placeholder, or operator data-entry error.

Resolution criterion: confirm intent at next seed Excel touch-point.
If intentional, document why. If error, flip to true.
```

Assign actual FN-AB numbers based on the highest existing FN-AB-N in CLAUDE.md (verify at edit time; expected to be FN-AB-53 or higher based on session memory).

### Bucket 4: docs/implementation-steps/step-6_18_1-roles-catalogue-2026-05-19.md NEW

```markdown
# Step 6.18.1 : ADMIN.ROLES.OVERRIDE.GLOBAL catalogue seed delta

## Mental Model

Two-row catalogue delta. Adds the new permission tuple that will gate
the future 6.18.3 PATCH endpoint, and grants it to SUPER_ADMIN. Mirrors
Step 6.17.1 pattern: Excel edit for local, inline SQL for Cloud SQL,
test count updates.

## Implementation Plan

1. Excel edit: permissions sheet +p40 (ADMIN.ROLES.OVERRIDE.GLOBAL);
   role_permissions sheet +r_super_admin x p40 grant.
2. Local seed: `uv run python -m scripts.seed_dev_data --reset`.
3. Cloud SQL: operator-run inline UPSERT in Cloud SQL Studio.
4. Test count updates in EXPECTED_VISIBLE_COUNTS_PLATFORM.
5. BUILD_PLAN + CLAUDE.md edits.

## Retro

Manual operator-driven step (no CC implementation in the catalogue
sub-step; CC handles only verification + text-file edits + commit).

- Local applied via Excel + seed loader. Cloud SQL applied via inline
  UPSERT.
- Cross-env audit-actor drift surfaced as FN-AB-NN (bootstrap user
  referenced locally; Anjali used in cloud).
- Cloud SQL Studio required schema-qualifying `core.uuidv7()` (function
  exists but not on default search_path). Mirrors known Cloud SQL
  search_path issue from earlier dashboard fix (FN-AB-19 / D-29 era).
- Excel ghost-row gotcha during edit: openpyxl tracked one extra blank
  row after the initial p40 entry causing first seed load to fail with
  NotNullViolation on a fully-blank row; resolved by clearing and
  re-entering the new row.
- Verification clean: both local and cloud at permissions=36,
  role_permissions=132, with SUPER_ADMIN holding the new permission.
- This step is a hard precondition for 6.18.3 PATCH endpoint's gate
  resolution; without it, the gate `(ADMIN, ROLES, OVERRIDE, GLOBAL)`
  has no catalogue row to resolve through.
```

## Verification harness (after edits)

```bash
# 1. seed loader test (load-bearing for this commit)
uv run pytest tests/integration/test_seed_loader.py -v --tb=short

# 2. Full suite regression
uv run pytest --tb=no -q | tail -3
# Expect: 611 passed (or current baseline; count assertions update only;
# no new tests in this commit).

# 3. mypy strict (verify no surprises)
uv run mypy --strict src/admin_backend 2>&1 | tail -3
# Expect: clean (no source changes).

# 4. check_setup
./scripts/check_setup.sh 2>&1 | tail -5
# Expect: 36/36 or whatever current baseline is.

# 5. Local DB final state (re-verify after all edits)
psql "${DATABASE_URL/postgresql+psycopg/postgresql}" -c "
SELECT 'permissions_total' AS check_name, COUNT(*)::text AS value FROM core.permissions
UNION ALL
SELECT 'role_permissions_total', COUNT(*)::text FROM core.role_permissions
UNION ALL
SELECT 'new_perm_row', code FROM core.permissions WHERE code = 'ADMIN.ROLES.OVERRIDE.GLOBAL'
UNION ALL
SELECT 'super_admin_holds_new_perm', COUNT(*)::text
  FROM core.role_permissions rp
  JOIN core.roles r ON r.id = rp.role_id
  JOIN core.permissions p ON p.id = rp.permission_id
  WHERE r.code = 'SUPER_ADMIN' AND p.code = 'ADMIN.ROLES.OVERRIDE.GLOBAL';
"
# Expect: 36 / 132 / ADMIN.ROLES.OVERRIDE.GLOBAL / 1
```

## Surface-and-stop scenarios

1. Pre-flight Check #2 returns counts other than 36 / 132. Stop. Report.
2. Pre-flight Check #4 reveals seed_loader test PASSES already (count assertions match current state). Surface; this means EXPECTED_VISIBLE_COUNTS_PLATFORM already reflects the new state, or counts elsewhere differ from the prompt's assumptions.
3. Pre-flight Check #5 cannot locate `EXPECTED_VISIBLE_COUNTS_PLATFORM` in `test_seed_loader.py`. Surface; the constant may have been renamed or moved.
4. mypy or pytest unexpected failures. Stop. Surface.
5. CLAUDE.md FN-AB-N numbering: if next-available number is unclear or conflicts, surface.
6. BUILD_PLAN.md location for new Step 6.18 section: if Step 6.17 entries don't end where expected, surface.

## Phase 5 exit: Report-Pause-Authorise before commit (A7)

After all edits + verification pass:

**Report (BEFORE staging):**

```
Step 6.18.1, DONE-LOCAL + CLOUD SQL APPLIED (pending operator authorisation to commit)

Files changed:
  Modified:
    data/ithina_dev_seed_data.xlsx (binary; operator-edited; +2 rows total)
    tests/integration/test_seed_loader.py (+2 count updates in EXPECTED_VISIBLE_COUNTS_PLATFORM)
    BUILD_PLAN.md (new Step 6.18 + 6.18.1 section)
    CLAUDE.md (one-line step doc pointer + 2 new FN-AB entries)
  New:
    docs/implementation-steps/step-6_18_1-roles-catalogue-2026-05-19.md

Verification:
  pytest test_seed_loader.py: PASS
  pytest full suite: 611 passed (or current baseline, unchanged)
  mypy strict: clean
  check_setup: NN/NN
  Local DB: permissions=36, role_permissions=132, new perm present, SUPER_ADMIN holds it
  Cloud SQL: already applied by operator (manual verification before commit)

Locked-decision honour record:
  LD1 (new permission code): Honoured.
  LD2 (SUPER_ADMIN sole initial holder): Honoured.
  LD3 (Excel + inline SQL, no Alembic): Honoured.
  LD4 (UUID via core.uuidv7()): Honoured (cloud required schema-qualification).
  LD5 (idempotent UPSERT): Honoured (re-running the SQL is a no-op).
  LD6 (test count assertions): Honoured (35 -> 36, 131 -> 132).

Surface-and-stop findings: 0 to N items.

Codebase observations beyond step scope:
  - FN-AB-NN added: cross-env audit-actor drift.
  - FN-AB-NN added: NIGHT_SHIFT_LEAD is_system=false intent.

Awaiting operator authorisation
  Staging command prepared but not run.
```

STOP. Wait for operator authorisation.

**After authorisation, stage and commit:**

```bash
git add \
  data/ithina_dev_seed_data.xlsx \
  tests/integration/test_seed_loader.py \
  BUILD_PLAN.md \
  CLAUDE.md \
  docs/implementation-steps/step-6_18_1-roles-catalogue-2026-05-19.md \
  prompts/step-6_18_1-impl-2026-05-19.md

# Verify staging
git status
git diff --cached --stat

# Commit
git commit \
  -m "Step 6.18.1: ADMIN.ROLES.OVERRIDE.GLOBAL catalogue delta + SUPER_ADMIN grant" \
  -m "Adds new permission ADMIN.ROLES.OVERRIDE.GLOBAL (module=ADMIN, resource=ROLES, action=OVERRIDE, scope=GLOBAL). Grants it to SUPER_ADMIN. Gates the future PATCH /api/v1/roles/{role_id} endpoint (6.18.3). SUPER_ADMIN is sole initial holder; PLATFORM_ADMIN may be granted later via the PATCH endpoint itself (recursive bootstrap)." \
  -m "Local: Excel +1 permission row (p40), +1 role_permissions row (r_super_admin x p40), test count update (permissions 35 -> 36, role_permissions 131 -> 132). Cloud SQL: applied via operator-run inline UPSERT in Cloud SQL Studio (schema-qualified core.uuidv7(); used anjali@ithina.ai as created_by_user_id since bootstrap@ithina.ai does not exist in cloud)." \
  -m "Both environments verified: permissions=36, role_permissions=132, SUPER_ADMIN holds ADMIN.ROLES.OVERRIDE.GLOBAL. Two FN-AB notes captured: cross-env audit-actor drift on catalogue inserts; NIGHT_SHIFT_LEAD is_system=false intent confirmation. Unblocks Step 6.18.2 (GET /api/v1/roles/{role_id} detail endpoint)."

# Do NOT push. Report git log -1 --stat.
git log -1 --stat
```

## Coordination

- Unblocks: Step 6.18.2 (GET detail endpoint).
- No cloud deploy from this commit (cloud catalogue already applied via operator inline SQL).
