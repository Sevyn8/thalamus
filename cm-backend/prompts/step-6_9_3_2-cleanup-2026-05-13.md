# Prompt — Step 6.9.3.2 cleanup (post-Phase-3 seed update)

> Generated 2026-05-13. Calibrated against codebase HEAD at commit
> `80911fa` ("Step 6.9.3.2: endpoint retrofit + anchor deps + gate
> discipline"). Pre-cleanup pytest: 305 passed + 1 failed + 13 xfailed.
>
> Paste this entire block into a fresh Claude Code session.

---

## Standing discipline (read first)

### On the code sketches in this prompt

Code blocks are STARTING POINTS, not the answer. You have live access
to the codebase. Where a better implementation exists, use it; surface
deviations in the report.

Locked decisions remain locked. Everything else is calibrated guidance.

### Documentation writing

Updates to CLAUDE.md must be technical, sharp, concise. State facts, 
active voice present tense, one sentence per fact. No meta-commentary, 
no adjectives that don't add information.

### Definition of done

Before reporting complete:
1. All tests pass: target state is 319 passed, 0 failed, 0 xfailed.
2. mypy strict clean on every file touched.
3. CLAUDE.md updates sharp.
4. Pre-commit checks (check_setup.sh, pytest, mypy) all pass.

---

## Context — why this step exists

Step 6.9.3.2 shipped 14 retrofitted endpoints + per-resource anchor
deps + mandatory-gate-discipline test at commit `80911fa`. Per the
data-seeding posture P-1/P-2/P-3, three seed-data updates were applied
post-commit by the operator (NOT in the 6.9.3.2 commit itself):

- `permissions` table: +1 row (`ADMIN.TENANTS.VIEW.TENANT`)
- `role_permissions` table: +2 rows
  (OWNER → `ADMIN.TENANTS.VIEW.TENANT`, OWNER → `ADMIN.ORG_NODES.VIEW.TENANT`)

Both local DB and Cloud SQL are at the Phase 3 applied state. Verified
counts: perm_total=31, rp_total=122.

The expected outcome was that 13 xfail-marked tests would flip to xpass
once the seed update applied. Two issues surfaced when re-running
pytest after the seed update:

**Issue 1: xfail tests did NOT flip to xpass.**
The 13 tests still xfail because they use `tenant_owner_jwt_factory`,
which builds a SYNTHETIC OWNER user with grants explicitly attached
in the factory (not read from the seeded OWNER role). The factory's
default `with_grants` list at HEAD is:
```python
[("ADMIN", "USERS", "VIEW", "TENANT")]
```
Missing: the two new tuples that just got added to the seeded OWNER
role. The factory's synthetic user therefore lacks the grants the
xfail tests need.

**Issue 2: test_seed_loader::test_l2_seed_row_counts now fails.**
This test asserts a known snapshot of seeded row counts:
```
AssertionError: permissions: expected 30 rows visible to PLATFORM, got 31
```
The expected count is stale after Phase 3.

### Scope of this cleanup

1. Update `tenant_owner_jwt_factory` default grants list to include the
   two new tuples (mirrors the seeded OWNER's actual grant coverage).
2. Update `test_l2_seed_row_counts` expected counts (permissions:
   30 → 31; role_permissions: 120 → 122 if asserted).
3. Remove `pytest.mark.xfail` markers from the 13 tests now that the
   factory provides the grants they need.
4. Update CLAUDE.md current-state entry for Step 6.9.3.2 to reflect
   Phase 3 applied + xfails resolved.
5. Section 6.9 final state in BUILD_PLAN.md (if needs adjustment).

Expected end state: 319 passed, 0 failed, 0 xfailed.

### Out of scope

- DDL changes
- Catalogue or role-grant changes beyond what was already applied via
  seed update
- New endpoints
- New behavioral tests
- Architecture.md (separate post-step task)

---

## Pre-flight

1. Run `./scripts/check_setup.sh`. Expect 35/35.
2. `git log --oneline -3`. Confirm HEAD is `80911fa` (Step 6.9.3.2).
3. `git status`. Expect clean (or surface anything unexpected).
4. `uv run alembic heads`. Expect `3e05299cb533` (no migration in this
   step).
5. Verify local DB has the Phase 3 state applied:
   ```sql
   SET search_path TO core, public;
   SELECT
     (SELECT COUNT(*) FROM permissions) AS perm_total,
     (SELECT COUNT(*) FROM role_permissions) AS rp_total;
   ```
   Expected: perm_total=31, rp_total=122. **If counts differ, stop
   and report** — the operator may not have applied seed updates yet
   on this DB.
6. Verify the 2 new OWNER grants exist:
   ```sql
   SELECT r.code AS role, p.code AS perm
   FROM role_permissions rp
   JOIN roles r ON r.id = rp.role_id
   JOIN permissions p ON p.id = rp.permission_id
   WHERE r.code = 'OWNER'
     AND p.code IN ('ADMIN.TENANTS.VIEW.TENANT', 'ADMIN.ORG_NODES.VIEW.TENANT');
   ```
   Expected: 2 rows.
7. Run `uv run pytest --tb=no -q | tail -5`. Confirm pre-cleanup state:
   - 1 test failed (`test_l2_seed_row_counts`)
   - ~305 tests passed
   - ~13 xfailed
   - Warning count not asserted (pre-existing 
     python-json-logger deprecation may vary by env)
   
   If failed count != 1 or xfailed count is significantly different 
   from 13 (>5 deviation either way), surface and pause.
8. Read `tests/conftest.py` fully. Locate `tenant_owner_jwt_factory`
   (per investigation: function-scoped fixture, builds synthetic
   OWNER user + role + role_permissions + assignment, mints JWT).
9. Read `tests/integration/test_seed_loader.py` fully. Locate
   `test_l2_seed_row_counts`. Note all hardcoded expected counts.
10. Find all 13 xfail markers via:
    ```bash
    grep -rn "pytest.mark.xfail" tests/integration/ | grep -i "Phase 3\|seed grant update\|6.9.3.2"
    ```
    Should return exactly 13 occurrences (or close — surface the
    actual list).
11. Read `CLAUDE.md`. Locate the Step 6.9.3.2 current-state entry
    and the FN-AB entries that may need updating.

12. **Factory consumer audit — required before editing.** Discover 
    all current consumers of `tenant_owner_jwt_factory`:
    
    ```bash
    grep -rn "tenant_owner_jwt_factory" tests/
    ```
    
    For each call site, classify intent:
    - Tests using factory's DEFAULT (no `with_grants=` override) — 
      these get expanded grant coverage post-update
    - Tests using EXPLICIT `with_grants=` override — unaffected by 
      default change
    
    For each default-using test, surface the test's intent:
    - Does the test rely on synthetic OWNER NOT having 
      ADMIN.TENANTS.VIEW.TENANT or ADMIN.ORG_NODES.VIEW.TENANT? 
      (e.g., "denial because OWNER lacks tuple X" tests)
    - If yes, that test would silently change behavior post-update 
      (start allowing instead of denying)
    - If no, the test is naturally unaffected (additional grants 
      don't change pass/fail for tests that don't gate on missing 
      grants)
    
    **Surface the audit table in the report BEFORE editing.** If 
    any default-using test would silently change behavior, surface 
    that test and pause for operator review.

---

## Step ID and intent

**Step 6.9.3.2 cleanup** — flips post-Phase-3 test state from
"305 passed + 1 failed + 13 xfailed" to "319 passed + 0 failed".

### Scope in

- MODIFIED: `tests/conftest.py` — update
  `tenant_owner_jwt_factory` default grants
- MODIFIED: `tests/integration/test_seed_loader.py` — update
  `test_l2_seed_row_counts` expected counts
- MODIFIED: ~5-7 test files — remove 13 `pytest.mark.xfail`
  markers (and their accompanying comments)
- MODIFIED: `CLAUDE.md` — Step 6.9.3.2 current-state entry
  reflects Phase 3 applied + xfails resolved
- MODIFIED: `BUILD_PLAN.md` — Section 6.9 final state (if needed)
- BUNDLED: `prompts/step-6_9_3_2-cleanup-2026-05-13.md` — this prompt

### Scope out

- New tests
- New gates
- New catalogue / grants
- Refactoring
- Architecture.md

### Acceptance criteria

- Pytest: 319 passed, 0 failed, 0 xfailed (or 0 xpassed if pytest
  collapses xpass into passed). Surface actual count.
- mypy strict clean on every file touched.
- check_setup 35/35.
- All 13 xfail markers removed.
- `tenant_owner_jwt_factory` default grants include the 2 new tuples.
- `test_l2_seed_row_counts` expected counts updated.
- CLAUDE.md current-state for 6.9.3.2 updated.
- No deviation from locked decisions.

### Locked decisions

1. Phase 3 seed updates are operator-applied; this commit reflects
   the state but does NOT re-apply them via fixtures or migrations.
2. Factory default grants extend to cover the three TENANT-scope 
   tuples needed by the currently-marked xfail tests:
     - ADMIN.USERS.VIEW.TENANT (existing in factory default)
     - ADMIN.TENANTS.VIEW.TENANT (added in this cleanup)
     - ADMIN.ORG_NODES.VIEW.TENANT (added in this cleanup)
   The default is NOT a mirror of seeded OWNER's full grant coverage 
   (OWNER holds ~20+ tuples in seed); it's a TARGETED extension to 
   unblock the 13 xfail tests. Other tests with specific grant 
   requirements continue to use explicit `with_grants=` overrides.
3. xfail markers are REMOVED (not converted to xpass-strict or kept
   as documentation). Tests pass cleanly post-cleanup.

---

## Implementation outline

### File 1: `tests/conftest.py` — MODIFY

Update `tenant_owner_jwt_factory`'s default grants list. Current
default (from Step 6.9.3.2 commit `80911fa`):

```python
# Current default (verify exact shape at HEAD)
default_grants = [("ADMIN", "USERS", "VIEW", "TENANT")]
```

Updated default (post-Phase-3, mirrors seeded OWNER's actual grant
coverage):

```python
default_grants = [
    ("ADMIN", "USERS", "VIEW", "TENANT"),
    ("ADMIN", "TENANTS", "VIEW", "TENANT"),      # added Phase 3
    ("ADMIN", "ORG_NODES", "VIEW", "TENANT"),    # added Phase 3
]
```

Inline comment for the new entries:

```python
# Updated 2026-05-13: Phase 3 seed update applied. Default grants
# now mirror the seeded OWNER role's actual coverage (the 2 new 
# tuples were granted to OWNER post-Step-6.9.3.2 via operator seed 
# update — see CLAUDE.md current state).
```

Verify other consumers of the factory don't break — some tests may
override `with_grants` explicitly, which continues to work.

**Scope caution — only `tenant_owner_jwt_factory` is modified.** 
Other fixtures (`super_admin_jwt`, any other JWT-minting helpers) 
are NOT touched. Surface in the report which conftest fixtures 
exist and confirm only `tenant_owner_jwt_factory` is modified.

### File 2: `tests/integration/test_seed_loader.py` — MODIFY

Update `test_l2_seed_row_counts`. The failure message states:
"permissions: expected 30 rows visible to PLATFORM, got 31". Find
the assertion(s) and update:

```python
# Find lines asserting permissions count
# Before:  expected 30; After: expected 31
# Before:  expected 120 (if asserted); After: expected 122
```

Surface in the report whether `role_permissions` count is also
asserted; if yes, update both. If no, only the permissions count
needs updating.

### Files 3-N: Test files with xfail markers — MODIFY

Remove all 13 `pytest.mark.xfail` markers + the `reason=` text 
inside the decorator referencing Phase 3 seed update. The
investigation suggested these may be in:
- `tests/integration/test_tenants_router.py` (a few)
- `tests/integration/test_org_tree_router.py` (a few)
- `tests/integration/test_dashboard_router.py` (most — 9 per the
  Claude Code audit)
- `tests/integration/test_modules_access_router.py` (a few)

**Endpoint → required tuple mapping (illustrative; actual counts 
from grep):**

```
~3 tests on /tenants/{id}                → ADMIN.TENANTS.VIEW.TENANT  (new)
~2 tests on /tenants/{id}/org-tree       → ADMIN.ORG_NODES.VIEW.TENANT (existing)
~2 tests on /tenants/{id}/org-nodes/...  → ADMIN.ORG_NODES.VIEW.TENANT (existing)
~9 tests on /dashboard/*                 → ADMIN.TENANTS.VIEW.TENANT (proxy)
~4 tests on /module-access/*             → ADMIN.TENANTS.VIEW.TENANT (proxy)
```

Sum is ~20 — but the actual landed xfail count was 13. The 
difference reflects that some tests used JWT-swap (to SUPER_ADMIN) 
or factory strategy during 6.9.3.2 implementation instead of 
xfail. The prompt CAN'T predict the exact distribution without 
inspecting actual markers; pre-flight item 10's grep provides 
the authoritative list.

All 3 grant categories needed (TENANTS.VIEW.TENANT, 
ORG_NODES.VIEW.TENANT) are covered by the factory default update 
in File 1. No additional grant work needed.

Surface the actual file-by-file count in the report. The grep
command in pre-flight item 10 identifies them.

For each marker:
- Remove the `@pytest.mark.xfail(...)` decorator entirely (including 
  multi-line `reason=` text inside the decorator).
- **Preserve the test function's docstring** (if any) — the docstring 
  describes what the test verifies; the xfail decorator described 
  the deferred state. These are independent.

```python
# Before:
@pytest.mark.xfail(
    reason=(
        "Needs operator seed grant update post-Step-6.9.3.2: "
        "ADMIN.TENANTS.VIEW.TENANT (new tuple) + OWNER grant. "
        "Operator un-skips once Phase 3b seed update applied."
    ),
    strict=False,
)
async def test_X(
    app_client, tenant_owner_jwt_factory, make_tenant
):
    """Test description docstring (PRESERVE THIS)."""
    ...

# After (remove the decorator, keep the docstring):
async def test_X(
    app_client, tenant_owner_jwt_factory, make_tenant
):
    """Test description docstring (PRESERVE THIS)."""
    ...
```

### File N+1: `CLAUDE.md` — MODIFY

**Append a NEW sub-bullet** under "Current state — Completed" for 
the cleanup. **Do NOT rewrite the existing 6.9.3.2 entry** — 
preserves the audit trail of the original commit vs the post-cleanup 
state.

New entry shape:

```
- Step 6.9.3.2 cleanup (this commit) — Phase 3 seed update applied 
  operator-side (permissions 30→31, role_permissions 120→122). 
  tenant_owner_jwt_factory default grants extended by 2 TENANT-scope 
  tuples (ADMIN.TENANTS.VIEW.TENANT, ADMIN.ORG_NODES.VIEW.TENANT). 
  N xfail markers removed across M test files (surface N and M 
  in report). test_l2_seed_row_counts expected counts updated. 
  Final state: 319 passed, 0 failed, 0 xfailed.
```

If any FN-AB entries reference Phase 3 as pending, update them to
reflect APPLIED state. Specifically check FN-AB-29 (Dashboard +
module-access dedicated tuples) and similar — these were forward
notes that may need wording adjustment now that Phase 3 is applied
(though the forward-question of "should dedicated tuples replace
the proxy" remains open; just the timing-context note may need
update).

### File N+2: `BUILD_PLAN.md` — MODIFY (if needed)

If BUILD_PLAN.md Section 6.9 has any reference to "pending seed
update" or similar follow-up text, mark as APPLIED. If Section 6.9
is already marked COMPLETE (per the 6.9.3.2 commit), no change
needed.

### File N+3: Prompt file — BUNDLE

`prompts/step-6_9_3_2-cleanup-2026-05-13.md` bundled into the commit
per per-step convention.

---

## Caution-first risks

1. **Factory default grants must MATCH seeded OWNER role.** If the
   factory's default diverges from what OWNER actually holds in seed,
   tests will pass under one configuration but the system would behave
   differently. Verify by running the SQL from pre-flight item 6
   AFTER the factory edit — the factory's default grants should be
   the exact list of TENANT-scope grants OWNER holds in seed.

2. **Some tests may have explicit `with_grants=` overrides** that
   intentionally use a smaller grant set (e.g., to test denial
   behavior for users WITHOUT a specific tuple). Don't change those
   tests' explicit overrides — only the FACTORY DEFAULT changes.

3. **xfail-strict=False allowed xpass to pass silently.** With the
   marker removed, tests must pass directly. If any test that was
   xpassed pre-cleanup now FAILS post-cleanup, surface — that's a
   real failure, not a marker-removal issue.

4. **The 13-test count is approximate.** The grep in pre-flight item
   10 returns the actual count. If it's >13 or <13, surface and
   investigate before proceeding.

5. **test_l2_seed_row_counts likely asserts multiple table counts as 
   a snapshot of seed state.** The Phase 3 update changed ONLY:
     - permissions: 30 → 31
     - role_permissions: 120 → 122
   
   All other counts (roles, tenants, tenant_users, platform_users, 
   modules, tenant_module_access, org_nodes, stores, etc.) MUST stay 
   unchanged. If Claude Code updates more than these 2 expected 
   counts, surface — the seed update only affected those 2 tables. 
   Surface the full set of asserted counts in the report.

6. **CLAUDE.md current-state entry update is additive.** Do NOT
   delete the existing 6.9.3.2 entry; append the cleanup state as
   a sub-bullet or follow-on entry. Preserves the audit trail of
   the original commit vs the post-cleanup state.

---

## Testing and regression discipline

### No new tests added

This is a cleanup commit. Existing behavioral tests already cover
the gate behavior (T_RET_1 through T_RET_8 from Step 6.9.3.2).
Removing xfail markers reactivates tests that were waiting on the
seed update; no new test surface is needed.

### Regression risk surface

1. **All 305 pre-cleanup passing tests should continue to pass.**
   Factory default change is additive (more grants); existing tests
   that depend on the default getting OWNER's `ADMIN.USERS.VIEW.TENANT`
   grant still get it.

2. **The 13 xfail tests should pass directly post-cleanup.** If any
   fails, the factory default is misaligned with the test's
   requirement; surface and investigate.

3. **The seed loader regression test passes with updated count.**
   If it fails again post-update, surface — the count expectation
   may have multiple flavors (e.g., visible-to-PLATFORM vs total)
   and need separate updates.

---

## Verification harness

```bash
# 0. Confirm Phase 3 applied to local DB.
psql "$DATABASE_URL" -c "
SET search_path TO core, public;
SELECT
  (SELECT COUNT(*) FROM permissions) AS perm,
  (SELECT COUNT(*) FROM role_permissions) AS rp;
"
# Expected: perm=31, rp=122

# 1. mypy strict.
uv run mypy src/admin_backend/

# 2. Full pytest.
uv run pytest --tb=no -q
# Expected: 319 passed, 0 failed, 0 xfailed (or 0 xpassed)

# 3. Targeted: previously xfail tests.
# Run the specific test files that had xfail markers and confirm 
# clean passing.
uv run pytest tests/integration/test_tenants_router.py \
              tests/integration/test_org_tree_router.py \
              tests/integration/test_dashboard_router.py \
              tests/integration/test_modules_access_router.py \
              --tb=no -q

# 4. Targeted: seed loader.
uv run pytest tests/integration/test_seed_loader.py -v

# 5. check_setup.
./scripts/check_setup.sh
# Expected: 35/35

# 6. Smoke test (post-truncate per convention). Recommended for 
# this cleanup since test infrastructure changes can subtly affect 
# what's exercised in CI vs smoke.
# 
# Truncate seed state if needed, then:
uv run python -m scripts.smoke_test
# Expected: 81/81 (post-truncate baseline established in Step 6.9.3.1).
```

---

## Report (BEFORE proposing commit)

1. Pre-flight outputs (items 1-11).
2. Files changed list:
   - tests/conftest.py (factory default update)
   - tests/integration/test_seed_loader.py (count update)
   - List of test files with xfail markers removed, count per file
   - CLAUDE.md
   - BUILD_PLAN.md (if applicable)
3. Exact xfail marker count found and removed.
4. Verification harness output (steps 0-5; step 6 optional).
5. Pre/post pytest counts: 305 passed + 1 failed + 13 xfailed →
   319 passed + 0 failed + 0 xfailed (or surface actual numbers).
6. Any deviation from locked decisions (should be none).
7. Surface: any tests that failed post-cleanup that were expected
   to pass.

Wait for explicit operator authorisation before staging or
committing.

---

## Surface-and-stop scenarios

Stop and report if:

1. Pre-flight item 5 shows perm_total ≠ 31 or rp_total ≠ 122
   (seed update not applied to local DB).
2. The xfail marker count differs from 13 by more than 5 (i.e., 
   anything outside the 8-18 range). The Claude Code audit had a 
   range of ~15-21 candidates before some converted to JWT-swap or 
   factory strategy during 6.9.3.2 implementation; 8-18 is a 
   reasonable tolerance band.
3. Removing xfail markers causes >0 test failures (factory default
   misaligned).
4. test_l2_seed_row_counts has more hardcoded counts than expected
   (e.g., asserts roles count too, which would also be wrong if
   it's a snapshot of full seed state).
5. Final pytest doesn't reach 319 passes.

---

## After completing

Propose a git commit per CLAUDE.md "After completing a task"
Pattern A:

```bash
git status
# Pre-flight item 10's grep identifies the actual files containing 
# xfail markers. The exact file list depends on where markers landed 
# during Step 6.9.3.2. Add files per actual changes:

git add tests/conftest.py \
        tests/integration/test_seed_loader.py \
        <list of test files where xfail markers were removed> \
        CLAUDE.md BUILD_PLAN.md \
        prompts/step-6_9_3_2-cleanup-2026-05-13.md

# Or simpler — use the broader "tests/" wildcard and let git status 
# show which files actually changed:
#
#   git add tests/ CLAUDE.md BUILD_PLAN.md \
#           prompts/step-6_9_3_2-cleanup-2026-05-13.md
#
# Either approach is acceptable; surface the final file list in 
# the commit message and final report.

git commit -m "$(cat <<'EOF'
Step 6.9.3.2 cleanup: Phase 3 seed update applied

- MODIFIED: tests/conftest.py. tenant_owner_jwt_factory default 
  grants list extended to include the 2 new TENANT-scope tuples 
  added to seeded OWNER role via operator Phase 3 seed update 
  (ADMIN.TENANTS.VIEW.TENANT new + ADMIN.ORG_NODES.VIEW.TENANT 
  existing). Factory now mirrors seeded OWNER's actual grant 
  coverage; tests using the factory's default get OWNER-equivalent 
  synthetic users.
- MODIFIED: tests/integration/test_seed_loader.py. 
  test_l2_seed_row_counts expected counts updated for Phase 3 
  state: permissions 30 → 31, role_permissions 120 → 122 (if 
  asserted; surface in report which counts changed).
- MODIFIED: N test files. 13 pytest.mark.xfail markers removed 
  (per-file count surfaced in report). Tests now pass directly 
  against the updated factory default.
- MODIFIED: CLAUDE.md. Step 6.9.3.2 current-state entry appended 
  with Phase 3 applied + xfails resolved sub-entry. FN-AB entries 
  that referenced Phase 3 as pending updated to reflect APPLIED 
  state where applicable.
- prompts/step-6_9_3_2-cleanup-2026-05-13.md bundled.

- pytest 305 passed + 1 failed + 13 xfailed → 319 passed + 0 failed 
  + 0 xfailed. mypy strict clean.
- check_setup 35/35.

Phase 3 seed updates were applied operator-side (Excel + local 
seed loader + Cloud SQL UPSERT) post-Step-6.9.3.2 commit (80911fa). 
This cleanup commit reconciles test infrastructure with the new 
seed state.

Section 6.9 fully closed.
EOF
)" && git status
```

Run? yes / no / edit message — awaiting authorisation.

---

## End of prompt
