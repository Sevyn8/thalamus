# Step 6.16.5 : Audit emission for module-access + org-tree + stores endpoints + GET resource_type filter (impl prompt)

## Standing discipline

- Project context docs (CLAUDE.md, BUILD_PLAN.md, docs/architecture.md, docs/architecture_RBAC.md, docs/architecture_audit_logs.md, WORKFLOW.md) are pre-loaded as project knowledge.
- `docs/architecture_audit_logs.md` (landed 6.16.0 `71351f4`; refined 6.16.2 `d194605`; extended 6.16.4 `eef95e4` with optional-sub-key convention) is the authoritative design reference. Implementation against this document; no re-decision of locked principles.
- 6.16.4's prompt (`prompts/step-6_16_4-impl-2026-05-21.md`) is the structural template. 6.16.5 mirrors it wherever possible; divergences are called out as locked decisions with rationale.
- One commit. A6 fold-in: code + tests + step doc + CLAUDE.md + BUILD_PLAN.md + design doc edits all bundle into the single Phase 5 commit.
- A9 pre-flight gate: report findings and pause for authorisation before code.
- A7 commit-hash gate at Phase 5 close: report-pause-authorise before staging or committing.
- A8 cite-or-verify discipline: every codebase claim is grep-verifiable.
- No em-dashes in any new handwritten content.
- No catalogue/seed changes.
- No new write endpoints; one GET extension (resource_type query param).
- This step CLOSES the 6.16 series.

### Contradiction-surfacing license

If any claim in this prompt contradicts what CC discovers via grep at pre-flight, CC reports the discrepancy and STOPS. The prompt loses to the live codebase; surface and wait for operator decision.

## Scope in (this commit)

Wire synchronous audit emission into 7 v0 write endpoints across 3 resource families, AND extend the existing 6.16.3 GET endpoint with a `resource_type` filter query parameter.

**Module-access (2 endpoints):**
- `POST /api/v1/module-access/{tenant_id}/{module_code}/enable`
- `POST /api/v1/module-access/{tenant_id}/{module_code}/disable`

**Org-tree (2 endpoints):**
- `POST /api/v1/tenants/{tenant_id}/org-tree` (add node)
- `PATCH /api/v1/tenants/{tenant_id}/org-tree/{node_id}` (edit node)

**Stores (3 endpoints):**
- `POST /api/v1/stores` (create; atomic-pair store + org_node when org_node not provided per 6.21.2)
- `PATCH /api/v1/stores/{store_id}` (update)
- `POST /api/v1/stores/{store_id}/set-status` (transition; 4 per-target action codes per LD3)

**GET endpoint extension:**
- `GET /api/v1/audit/activities` gains `resource_type: str | None = None` query parameter; AND-composed with existing filters; passes through to both UNION branches at the repo. Frontend integration deferred (wire surface only).

Both success and failure paths emit audit rows per design doc emission contract. All 7 endpoints route to `tenant_activity_audit_logs` (tenant_id always known; no platform-table routing in this step).

Bundle four design doc edits (per A6 / LD19):

1. **FN-AB-66 closure**: codify the per-route extractor mapping in `docs/architecture_audit_logs.md` Emission contract section. Short paragraph framing the extractor convention (each route declares its identifying path-param key and its resource_label lookup source).
2. **Resource_type filter mention**: add a short note in the Read contract section that the GET list endpoint accepts `resource_type` as a filter.
3. **Sub-step plan table closure**: flip 6.16.5 TODO -> DONE-LOCAL; add a closing note that the 6.16 series is complete (all 16 v0 write endpoints emit audit rows).
4. **Resource_type vocabulary table**: list the six values now in use (`TENANT`, `TENANT_USER`, `ROLE`, `MODULE_ACCESS`, `ORG_NODE`, `STORE`) with their source step references.

## Scope out

- Frontend `resource_type` filter UI integration (wire surface lands; frontend work tracked separately).
- Async outbox pattern (design doc Scale Considerations defers).
- JSONB GIN index, trigram search index, actor BTREE index, partition tables (all deferred per design doc).
- Promotion of `_actor_type_from_auth` to shared module (FN-AB-58 stays open).
- FN-AB-63 Pydantic 422 bypass (affects all endpoints uniformly; out of scope per established deferral).
- FN-AB-67 actor enrichment (full_name + role snapshot; DDL change required).
- New permission catalogue rows; gates used by these 7 endpoints were seeded at 6.13, 6.15, 6.17.1.
- Org-tree edit-node action vocabulary divergence (LD9: always "UPDATE" regardless of which fields changed).
- Orphan-detection inline flag on store PATCH org_node_id change (LD6).
- Downstream impact summary for org-tree edits (LD4).
- CSV export for audit log.
- Smoke / test_endpoint script changes (no new public write surface; the GET query param addition is acceptable as a small smoke extension but optional).

## Locked decisions

| # | Decision | Owner artifact |
|---|----------|----------------|
| LD1 | Extend `AUDITED_ROUTES` in `src/admin_backend/audit/emit.py` with 7 new entries: 2 module-access (ENABLE / DISABLE; resource_type `"MODULE_ACCESS"`; route_to_platform=False), 2 org-tree (CREATE / UPDATE; resource_type `"ORG_NODE"`; route_to_platform=False), 3 stores (POST=CREATE / PATCH=UPDATE / set-status=`SET_STATUS`; resource_type `"STORE"`; route_to_platform=False). The stores set-status entry carries action `"SET_STATUS"` as its failure-path action code per LD3; success-path emission overrides this with one of the 4 per-target codes by passing the resolved action directly to `emit_audit_event` from the repo (success path does not consult AUDITED_ROUTES for the action). | emit.py |
| LD2 | Module-access ENABLE single action code with `before.status` distinguishing first-time vs re-activation (Phase 1 Q1). `before.status = null` on first-time INSERT path; `before.status = "DISABLED"` on re-enable. DISABLE is symmetric: `before.status = "ENABLED"`, `after.status = "DISABLED"`. **No-op idempotent paths (enable-on-ENABLED, disable-on-DISABLED) are NOT audited** (Phase 1 Q10; closes FN-AB-42). The repo detects the no-op condition (current status equals target status) and skips emission entirely. Refines 6.16.4 LD14's "one row per HTTP request" invariant to "at most one row per HTTP request"; emission is conditional on the request causing a state change or a failure outcome. | repositories/modules_access.py |
| LD3 | Stores set-status dispatches on `target_status` to pick one of 4 action codes at emission time on **success path**: target=OPENING -> `"OPEN_SOFT"` / `"Soft-opened"`; target=ACTIVE -> `"ACTIVATE"` / `"Activated"` (reused from tenants); target=CLOSED -> `"CLOSE"` / `"Closed"`; target=INACTIVE -> `"DEACTIVATE"` / `"Deactivated"`. **Failure path** uses single fallback action code `"SET_STATUS"` / `"Status change"` (one new code) because the failure-path handler cannot reliably re-parse the body to determine `target_status` (FastAPI request body consumed by handler entry). Asymmetric but principled: success-path action describes what happened; failure-path action describes what was attempted. Standard transition-shape payload `{before: {status: ...}, after: {status: ...}}` on success regardless of action code (Phase 1 Q3). `_label_for_action` gains 4 new entries (`OPEN_SOFT`, `CLOSE`, `DEACTIVATE`, `SET_STATUS`); `ACTIVATE` reused as-is. | emit.py + repositories/stores.py |
| LD4 | Org-tree edit-node always uses action `"UPDATE"` regardless of which fields changed (Phase 1 Q9). Status archive/unarchive visible in `details.before` / `details.after` status diff, not in action code. Principle: action codes are per-endpoint-shape, not per-field-change. | repositories/org_nodes.py |
| LD5 | Org-tree CREATE row's `details.snapshot` carries the new node's identity fields PLUS `parent_org_node_name` frozen at write time (so the auditor sees where the node was added). Org-tree UPDATE row's `details.before` / `details.after` carry whichever fields changed; if `parent_id` changed (reparent), both old and new parent's `parent_org_node_name` snapshotted in respective halves. Audit row self-contained; no descendants-count or path-depth summary (Phase 1 Q4). | repositories/org_nodes.py |
| LD6 | Atomic-pair store creation: one audit row per HTTP request preserved (Phase 1 Q5). The row's `resource_type: "STORE"`, `resource_id: <store_id>`, `resource_label: <store_name>`. `details.snapshot` carries store identity fields + `org_node_id` + `org_node_name` (frozen) + `org_node_created_atomically: true` flag when the atomic-pair path fired (so the auditor sees the org_node was newly created, not pre-existing). When the org_node is pre-existing (linked, not atomically-created), `org_node_created_atomically: false`. | repositories/stores.py |
| LD7 | Store PATCH `org_node_id` change recorded mechanically (Phase 1 Q6). Both old and new org_node ids + names snapshotted in `details.before` / `details.after`. Orphan status of the previous org_node NOT surfaced inline; auditor correlates via org-tree if needed. | repositories/stores.py |
| LD8 | `resource_type` vocabulary: `"MODULE_ACCESS"` (not `"TENANT_MODULE_ACCESS"`; Phase 1 Q8 prefers brevity when table-name has redundant context). `"ORG_NODE"` and `"STORE"` follow the established UPPERCASE_SINGULAR table-derived convention. | emit.py |
| LD9 | `resource_label` snapshot: module-access uses the module's display label from `core.lookups` keyed by `list_name='module_code'` AND `code=:mc` (Phase 1 Q2); org-tree uses `org_nodes.name`; stores uses `stores.name`. Module-access label resolution adds a new lookup table (third pattern; triggers FN-AB-66 closure per LD12). | emit.py + repo callers |
| LD10 | `tenant_id` and `tenant_name` population: module-access reads `tenant_id` from path; org-tree reads `tenant_id` from path; stores reads `tenant_id` from the store's row on success path. **Failure path for POST /stores**: body is consumed by FastAPI before the failure handler fires; the extractor attempts to read `tenant_id` from `request.state` if any middleware caches body fields (verify at pre-flight), else falls back to platform-table routing with `tenant_id=NULL` per 6.16.4's POST /tenant-users precedent. `tenant_name` always resolved via JOIN to `core.tenants` with defensive `<unknown>` fallback if concurrently deleted (mirrors 6.16.2 + 6.16.4 failure-path behavior). Document at SF1 which routing the test asserts (likely platform-table-fallback). | repo callers + emit.py + main.py extractor |
| LD11 | SUCCESS row `details` payload shape per design doc: `{snapshot: {...}}` for CREATE-shaped actions (org-tree add-node, stores POST); `{before: {...}, after: {...}}` for UPDATE / ENABLE / DISABLE / set-status transitions. For org-tree PATCH where multiple fields changed, all changed fields appear in both halves (full diff per field). | details builders |
| LD12 | **FN-AB-66 closure**: replace the minimal multi-key path-param fallthrough at `main.py:347` (introduced by 6.16.4) with a per-route extractor mapping. Shape choice goes to CC per "spec what, not how" — two reasonable shapes: (a) extend the AUDITED_ROUTES tuple with a 4th element (extractor callable); (b) sibling dict `RESOURCE_EXTRACTORS: dict[resource_type, ExtractorFunc]` consulted after AUDITED_ROUTES lookup. **Lean toward (b)** because extractors are per-resource_type (one extractor handles all routes for STORE; same for ORG_NODE etc.); (a) duplicates extractor refs across each route's entry. If CC sees a third reasonable shape, surface. The extractor function signature accepts the Request object + the exception and returns a small dataclass or tuple carrying `(resource_id | None, tenant_id | None, resource_label | None)`. **Preserve 6.16.4 iteration-2 extension**: `_failure_result_and_details` gained an `auth` kwarg at 6.16.4 to fall back caller_audience to auth.user_type for PERMISSION_DENIED rows where context is empty. The LD12 rewrite must preserve this fallback; it is load-bearing for the AF-series tests. The 6.16.4-shipped 5 routes' extractor logic migrates to the new shape; 6.16.5's 7 new routes' extractors land alongside. Surface the chosen shape in the Phase 5 report. | main.py + emit.py |
| LD13 | Failure-path `details` payload carries error context ONLY; no attempted-change snapshot (consistent with 6.16.4 LD10 / Phase 1 Q6). For INVALID_STATE_TRANSITION on stores set-status, `value` is the *current* status that blocked the transition, not the attempted target. **Anchor-404 paths are NOT audited** (mirrors 6.16.2 AF6 / 6.16.4 AF8 precedent): module-access disable on missing row -> 404 MODULE_ACCESS_NOT_FOUND -> 0 audit rows; org-tree PATCH on nonexistent node_id -> 404 ORG_NODE_NOT_FOUND -> 0 audit rows; stores PATCH / set-status on nonexistent store_id -> 404 STORE_NOT_FOUND -> 0 audit rows. **Non-404 failures ARE audited**: module-access enable has an upsert seam (no row-not-found failure exists on enable; permission/validation denials are audited with `resource_id=NULL` and `resource_label` resolved via `core.lookups` by module_code alone). | details builders |
| LD14 | **At most one** audit row per HTTP request (refines 6.16.4 LD14). No-op idempotent paths (LD2) produce zero rows. Atomic-pair stores POST emits one row (LD6). Diff-replace on PATCH endpoints (if any future use surfaces) emits one row. Emission is conditional on the request causing a state change or a failure outcome. | All emission paths |
| LD15 | Repo method signature evolution mirrors 6.16.4 LD15: `auth: AuthContext | None = None` and `request_id: UUID | None = None` as optional kwargs on every modified repo method. Mixing one without the other raises ValueError. Both-omitted skips emission (repo-level unit tests). Affected methods: `ModulesAccessRepo.enable` / `disable`; `OrgNodesRepo.create` / `update`; `StoresRepo.create` / `update` / `transition`. | repo signatures |
| LD16 | `_actor_type_from_auth` reused from `audit/emit.py` (per 6.16.4 LD16; FN-AB-58 stays open; no 5th local copy). | emit.py (no change) |
| LD17 | GET endpoint `resource_type` filter: new optional query param on `GET /api/v1/audit/activities`. Pydantic type `str | None = None`; no Literal restriction (resource_type is "free-text vocabulary" per design doc). AND-composed with existing filters at the repo SQL builder. Both UNION branches (tenant + platform) receive the filter. Unknown values produce 0 rows naturally (no WHERE-match), not 422. | schemas/audit_log.py + repositories/audit_logs.py + routers/v1/audit.py |
| LD18 | Test catalogue: 4 new test files mirroring 6.16.4's per-resource structure: `test_audit_emission_module_access.py` (~12 tests across success + failure), `test_audit_emission_org_tree.py` (~12 tests), `test_audit_emission_stores.py` (~16 tests; larger due to 3 endpoints + atomic-pair coverage), `test_audit_router_resource_type_filter.py` (~5 tests for the GET extension). Existing `test_audit_emit.py` extended with ~2 unit tests for the new builder behaviours surfaced by LD3 (per-target action label dispatch) and LD6 (snapshot with `org_node_created_atomically` flag). | test files |
| LD19 | Test fixture cleanup: leverages 6.16.4 iteration-3's `make_tenant` teardown extension which DELETEs audit rows ahead of tenant DELETE. All three new resource families' test data inherits this cleanly; no per-test fixture extensions needed. Verify at pre-flight that the make_tenant teardown extension is still in place. | conftest.py (no change expected) |
| LD20 | Four design doc edits bundled per A6 (LD19 of 6.16.4 generalises to LD20 here): (a) FN-AB-66-closure paragraph in Emission contract; (b) resource_type filter mention in Read contract; (c) sub-step plan table closure (6.16.5 -> DONE-LOCAL; series complete note); (d) resource_type vocabulary table. Framing prose in Appendix A; CC composes the exact insertion location and surrounding edit shape at apply time. | docs/architecture_audit_logs.md + BUILD_PLAN.md |

## Pre-flight (A9; per-check report required)

CC produces a per-check report, transcribes actual output for each, and STOPS. Wait for operator authorisation before any edit.

```
Pre-flight for Step 6.16.5

Check #1: prior commit + working tree
  Command: git log -1 --oneline && git status --short
  Expected: HEAD = eef95e4 (Step 6.16.4 commit) or a later compatible commit. Tree clean or only this prompt's untracked file.
  Output: <transcript>
  Status: matches / deviates

Check #2: pytest baseline
  Command: uv run pytest --tb=no -q | tail -3
  Expected: 824 passed (Step 6.16.4 closure baseline).
  Output: <transcript>
  Status: captured

Check #3: AUDITED_ROUTES current contents
  Command: grep -A 30 "AUDITED_ROUTES" src/admin_backend/audit/emit.py | head -40
  Expected: 9 entries (4 tenants + 5 from 6.16.4: 4 tenant-users + 1 roles). No module-access, org-tree, or stores entries.
  Output: <transcript>
  Status: matches / deviates
  Surface: STOP if any of the 7 new routes already present.

Check #4: existing audit module shape post-6.16.4
  Command: ls -la src/admin_backend/audit/ && grep -n "^def \|^class " src/admin_backend/audit/emit.py
  Expected: __init__.py + emit.py. Builders present per 6.16.4 + 6.16.2 conventions. Capture full function list.
  Output: <transcript>
  Status: matches / deviates

Check #5: per-route extractor mapping shape post-6.16.4
  Command: grep -B 2 -A 30 "_failure_result_and_details\|path_params.get\|resource_label\|_emit_failure_audit_if_audited" src/admin_backend/main.py | head -80
  Expected: 6.16.4's multi-key fallthrough (tenant_id -> user_id -> role_id) and resource_type-dispatch on resource_label lookup (TENANT / TENANT_USER / ROLE). 6.16.5 replaces this with the LD12 extractor mapping.
  Output: <transcript>
  Status: captured

Check #6: existing ModulesAccessRepo + OrgNodesRepo + StoresRepo method signatures
  Command: grep -B 1 -A 5 "async def enable\|async def disable" src/admin_backend/repositories/modules_access.py
           grep -B 1 -A 5 "async def create\|async def update" src/admin_backend/repositories/org_nodes.py
           grep -B 1 -A 5 "async def create\|async def update\|async def transition" src/admin_backend/repositories/stores.py
  Expected: all 7 methods exist with current actor_user_id + actor_user_type signatures. LD15 adds auth + request_id optional kwargs.
  Output: <transcript>
  Status: captured

Check #7: lookups table query pattern for module display label
  Command: grep -rn "list_name.*module_code\|core.lookups" src/admin_backend/ tests/ | head -20
  Expected: at least one existing query pattern exists for resolving `module_code` -> display label from `core.lookups`. Capture the exact shape (likely `permission_matrix.py` precedent per 6.18.2 LD4). 6.16.5 reuses the pattern.
  Output: <transcript>
  Status: captured

Check #8: stores atomic-pair POST detection
  Command: grep -B 2 -A 20 "atomic\|org_node_created\|_create_atomic" src/admin_backend/repositories/stores.py | head -40
  Expected: 6.21.2's atomic-pair create logic identifiable. Capture the boolean / detection mechanism so LD6's `org_node_created_atomically` flag can be set correctly.
  Output: <transcript>
  Status: captured

Check #9: make_tenant teardown extension intact (6.16.4 iteration 3)
  Command: grep -B 2 -A 10 "def make_tenant\|teardown.*audit" tests/integration/conftest.py | head -40
  Expected: make_tenant fixture's teardown DELETEs audit rows before tenant DELETE. If absent, surface — LD19 assumed it's in place.
  Output: <transcript>
  Status: matches / deviates

Check #10: GET endpoint current query param structure (audit router)
  Command: grep -B 2 -A 30 "list_audit_activities\|@router.get" src/admin_backend/routers/v1/audit.py | head -60
  Expected: existing query params (cursor, limit, from_, to, status, tenant_id, scope, search). LD17 adds resource_type.
  Output: <transcript>
  Status: matches / deviates

Check #11: AuditLogsRepo.list current signature + SQL builder shape
  Command: grep -B 1 -A 15 "async def list\|_build_tenant_branch_sql\|_build_platform_branch_sql\|_apply_common_filters" src/admin_backend/repositories/audit_logs.py | head -60
  Expected: existing filter composition. LD17 adds resource_type WHERE clause to both branches.
  Output: <transcript>
  Status: matches / deviates

Check #12: existing diff-replace logic on org-tree PATCH
  Command: grep -B 1 -A 10 "_select_subtree\|_rewrite_paths\|reparent" src/admin_backend/repositories/org_nodes.py | head -40
  Expected: 6.13's reparent + subtree-path-rewrite logic present. The audit emission integrates AFTER the path rewrites are applied.
  Output: <transcript>
  Status: matches / deviates

Check #13: stores TRANSITION_MATRIX + 4 target action codes mapping
  Command: grep -B 2 -A 15 "TRANSITION_MATRIX\|StoreStatus" src/admin_backend/repositories/stores.py | head -40
  Expected: 6.17.4's TRANSITION_MATRIX present. Capture the StoreStatus enum values to confirm LD3's mapping (OPENING -> OPEN_SOFT; ACTIVE -> ACTIVATE; CLOSED -> CLOSE; INACTIVE -> DEACTIVATE).
  Output: <transcript>
  Status: captured

Check #14: anchor-404 audit posture for module-access disable + org-tree PATCH + stores PATCH/set-status
  Command: grep -rn "class ModuleAccessNotFoundError\|class OrgNodeNotFoundError\|class StoreNotFoundError\|MODULE_ACCESS_NOT_FOUND\|ORG_NODE_NOT_FOUND\|STORE_NOT_FOUND" src/admin_backend/ | head -20
  Expected: error classes defined in errors.py; raise sites in the relevant routers/repos. The 6.16.2 AF6 / 6.16.4 AF8 precedent sets that anchor-404 paths are NOT audited. LD13 follows the same posture for all 3 resource families.
  Output: <transcript>
  Status: matches / deviates / ambiguous

Check #15: Next-available FN-AB number
  Command: grep -oE "FN-AB-[0-9]+" CLAUDE.md | sort -u -V | tail -5
  Expected: 6.16.4 closure landed FN-AB-65, 66, 67. 6.16.5 closes 65 and 66; no new FN-AB introduced unless impl surfaces one.
  Output: <transcript>
  Status: captured

Check #16: BUILD_PLAN.md 6.16.5 + 6.16 root entry state
  Command: grep -A 5 "^### Step 6.16.5\|^## Step 6.16 \|^### Step 6.16 " BUILD_PLAN.md | head -30
  Expected: 6.16.5 status TODO; 6.16 root references "16 endpoints across 6 resource families" (LD20 closes the series).
  Output: <transcript>
  Status: matches / deviates

Check #17: Per-resource regression baseline
  Command: ls -la tests/integration/test_module_access_writes_router.py tests/integration/test_org_tree_writes_router.py tests/integration/test_stores_writes_router.py tests/integration/test_stores_set_status_router.py 2>&1 | head -10
           for f in tests/integration/test_tenants_writes_router.py tests/integration/test_tenant_users_writes_router.py tests/integration/test_module_access_writes_router.py tests/integration/test_org_tree_writes_router.py tests/integration/test_stores_writes_router.py tests/integration/test_stores_set_status_router.py tests/integration/test_rbac_writes_router.py tests/integration/test_audit_router.py tests/integration/test_audit_logs_repo.py tests/integration/test_audit_emission_tenants.py tests/integration/test_audit_emission_failures.py tests/integration/test_audit_emission_tenant_users.py tests/integration/test_audit_emission_failures_tenant_users.py tests/integration/test_audit_emission_roles.py tests/unit/test_audit_emit.py; do [ -f "$f" ] && echo "$f: $(uv run pytest --collect-only -q $f 2>/dev/null | tail -1)" || echo "$f: MISSING"; done
  Expected: all listed files exist; each emits its current test count. Surface any MISSING line before proceeding (catches stale assumptions about file naming).
  Output: <transcript>
  Status: captured

Check #18: Symbol pre-existence grep for new emission code
  Command: grep -rn "build_module_access\|emit_module_access\|emit_org_node\|emit_store" src/admin_backend/ 2>/dev/null
  Expected: NO matches (no per-resource builders; LD11 reuses 6.16.4 builders with new kwargs). If any matches, surface — suggests prior partial work.
  Output: <transcript>
  Status: clean / deviates
```

## File-by-file change list (apply after pre-flight authorisation)

### 1. `src/admin_backend/audit/emit.py` (MODIFY)

Five changes:

- **AUDITED_ROUTES extension** per LD1: append 7 entries:
  - `("POST",  "/api/v1/module-access/{tenant_id}/{module_code}/enable")` -> `("ENABLE", "MODULE_ACCESS", False)`
  - `("POST",  "/api/v1/module-access/{tenant_id}/{module_code}/disable")` -> `("DISABLE", "MODULE_ACCESS", False)`
  - `("POST",  "/api/v1/tenants/{tenant_id}/org-tree")` -> `("CREATE", "ORG_NODE", False)`
  - `("PATCH", "/api/v1/tenants/{tenant_id}/org-tree/{node_id}")` -> `("UPDATE", "ORG_NODE", False)`
  - `("POST",  "/api/v1/stores")` -> `("CREATE", "STORE", False)`
  - `("PATCH", "/api/v1/stores/{store_id}")` -> `("UPDATE", "STORE", False)`
  - `("POST",  "/api/v1/stores/{store_id}/set-status")` -> `("SET_STATUS", "STORE", False)` — failure-path action code. Success path overrides with the per-target code (`OPEN_SOFT` / `ACTIVATE` / `CLOSE` / `DEACTIVATE`) per LD3 by passing the resolved action directly to `emit_audit_event` from `StoresRepo.transition`. The AUDITED_ROUTES entry's action is consulted only by the failure-path handler.

- **`_label_for_action` extension** per LD3: add 4 new entries: `OPEN_SOFT` -> `"Soft-opened"`, `CLOSE` -> `"Closed"`, `DEACTIVATE` -> `"Deactivated"`, `SET_STATUS` -> `"Status change"`. `ACTIVATE` reused (already present); `ENABLE` / `DISABLE` need labels too (`"Enabled"` / `"Disabled"`).

- **Per-route extractor mapping per LD12 (FN-AB-66 closure)**: replace the 6.16.4 multi-key fallthrough + resource_type-dispatch with a per-route extractor map. Shape choice per LD12. The extractor function signature: takes Request + exception (failure path) or takes the row + emission kwargs (success path) — but success-path emission already runs inside the repo with all values in hand, so the extractor primarily addresses the failure path. CC's design call on whether to unify both paths through one extractor or only extract on failure path. Surface the design call in Phase 5 report.

  Extractors required for 6.16.5's new resource_types:
  - `MODULE_ACCESS`: resource_id from `tenant_module_access` row lookup by `(tenant_id, module_code)` path params; resource_label from `core.lookups` by `(list_name='module_code', code=module_code)`; tenant_id from path.
  - `ORG_NODE`: resource_id from path `{node_id}` (for PATCH) or from inserted row (for POST); resource_label from `org_nodes.name` lookup; tenant_id from path `{tenant_id}`.
  - `STORE`: resource_id from path `{store_id}` (for PATCH / set-status) or from inserted row (for POST); resource_label from `stores.name` lookup; tenant_id from `stores.tenant_id` lookup (or from body on failed POST per LD10).

- **Details-builder extensions per LD11 + LD3 + LD6**:
  - CREATE-shape builder accepts optional `org_node_created_atomically: bool | None` kwarg for stores POST (LD6); when present, includes in snapshot.
  - CREATE-shape builder accepts optional `parent_org_node_name: str | None` kwarg for org-tree CREATE (LD5); when present, includes in snapshot.
  - UPDATE-shape builder for org-tree accepts optional `parent_org_node_name` in both before/after halves when `parent_id` changed (LD5).
  - Transition-shape builder for stores set-status: same `{before: {status}, after: {status}}` shape regardless of which of the 4 action codes fired (LD3); no builder change beyond what 6.16.4 already provides.
  - Module-access enable/disable: UPDATE-shape with `{before: {status}, after: {status}}`; `before.status = None` on first-time enable INSERT (LD2). Builder accepts `None` cleanly (JSONB serializes as `null`).

- **`actor_display_name`**: unchanged (auth.email per 6.16.4 LD13 / Option B).

### 2. `src/admin_backend/repositories/modules_access.py` (MODIFY)

Two methods modified: `enable`, `disable`. Each gains:

- **Optional kwargs** per LD15: `auth: AuthContext | None = None`, `request_id: UUID | None = None`.
- **Eager label resolution** per LD9: BEFORE the audit emission call, resolve the module's display label from `core.lookups` (one SELECT per call; sub-millisecond at v0 scale; pattern lifted from `permission_matrix.py` precedent per pre-flight Check #7).
- **Audit emission call** per LD2: build the before/after status payload. For first-time INSERT, `before.status = None`. For re-enable from DISABLED, `before.status = "DISABLED"`. For no-op idempotent paths (enable-on-ENABLED, disable-on-DISABLED), `before.status == after.status` — audit row still emits per the per-HTTP-request invariant.

When `auth=None` and `request_id=None`, skip emission cleanly (repo-level unit tests).

### 3. `src/admin_backend/repositories/org_nodes.py` (MODIFY)

Two methods modified: `create` (or whatever 6.13 named the add-node method; verify at pre-flight), `update`.

- **Optional kwargs** per LD15.
- **Eager parent_org_node_name resolution** per LD5: for CREATE, resolve the parent's name. For UPDATE where `parent_id` changed, resolve both old and new parents' names. Both reads inside the same transaction; session cache likely already holds these rows from pre-checks.
- **Audit emission call**:
  - CREATE: build snapshot with new node's identity fields + parent_org_node_name.
  - UPDATE: build before/after with whichever fields changed; if `parent_id` changed, include `parent_org_node_name` in both halves.

### 4. `src/admin_backend/repositories/stores.py` (MODIFY)

Three methods modified: `create`, `update`, `transition`.

- **Optional kwargs** per LD15.
- **Eager org_node_name resolution** per LD9 / LD7: for CREATE, capture the org_node's name (whether pre-existing or atomically created). For UPDATE where `org_node_id` changed, resolve both old and new names.
- **Atomic-pair detection** per LD6: capture whether the org_node was newly created in the same transaction (boolean flag derived from the 6.21.2 atomic-pair path; mechanism identified at pre-flight Check #8).
- **Audit emission calls**:
  - CREATE: snapshot with store identity fields + org_node_id + org_node_name + `org_node_created_atomically: true|false`.
  - UPDATE: before/after with changed fields; org_node_id change includes both names per LD7.
  - TRANSITION: action code dispatched per LD3 from `target_status`. Standard `{before: {status}, after: {status}}` payload.

### 5. `src/admin_backend/routers/v1/module_access.py` (MODIFY)

Thread `auth` + `request_id` (from `request.state` per existing convention) into `ModulesAccessRepo.enable` / `disable` calls. Handler logic unchanged.

### 6. `src/admin_backend/routers/v1/org_tree.py` (MODIFY)

Thread `auth` + `request_id` into `OrgNodesRepo.create` / `update` calls. Handler logic unchanged.

### 7. `src/admin_backend/routers/v1/stores.py` (MODIFY)

Thread `auth` + `request_id` into all three `StoresRepo` method calls. Handler logic unchanged.

### 8. `src/admin_backend/routers/v1/audit.py` (MODIFY)

Add `resource_type: str | None = Query(None)` parameter to the list endpoint signature. Pass through to `AuditLogsRepo.list(...)`.

### 9. `src/admin_backend/repositories/audit_logs.py` (MODIFY)

Extend `list` method signature with `resource_type: str | None = None` kwarg. Add WHERE clause in both UNION branches: `AND resource_type = :resource_type` when filter is present. Decide-or-surface whether the parameter binds at the SQL builder or inside `_apply_common_filters`.

### 10. `src/admin_backend/schemas/audit_log.py` (MODIFY)

If query params are declared as a Pydantic model (per 6.16.3 LD5's "optional convenience class"), add `resource_type: str | None = None` field. If they're inline at router-level, no schema change needed. Surface at pre-flight Check #10.

### 11. `src/admin_backend/main.py` (MODIFY)

Apply the LD12 per-route extractor mapping rewrite. Replace the 6.16.4 fallthrough + resource_type-dispatch logic with the chosen shape (LD12 leaves the exact form to CC).

### 12. `tests/unit/test_audit_emit.py` (MODIFY)

Add ~2 new unit tests:

- AE10: CREATE-shape builder includes `org_node_created_atomically` and `parent_org_node_name` sub-keys when supplied; omits when not.
- AE11: action-code dispatch for stores set-status: when builder is called with `action="OPEN_SOFT"`, action_label resolves to `"Soft-opened"`; same for `CLOSE` -> `"Closed"`, `DEACTIVATE` -> `"Deactivated"`, `ACTIVATE` -> `"Activated"`.

LOAD-BEARING: AE11 (the per-target action code dispatch is the user-facing contract for stores set-status).

### 13. `tests/integration/test_audit_emission_module_access.py` (NEW; ~12 tests)

Per-endpoint success + failure for module-access. Mirror 6.16.4 structure.

- MS1: POST enable on no-existing-row -> 1 audit row in `tenant_activity_audit_logs`, action=`ENABLE`, resource_type=`MODULE_ACCESS`, `details.before.status = null`, `details.after.status = "ENABLED"`.
- MS2: POST enable on DISABLED row -> 1 row, `before.status = "DISABLED"`, `after.status = "ENABLED"`.
- MS3: POST enable on ENABLED row (idempotent no-op) -> 0 audit rows per LD2 / Phase 1 Q10 (no-op not audited).
- MS4: POST disable on ENABLED row -> 1 row, action=`DISABLE`, `before.status = "ENABLED"`, `after.status = "DISABLED"`.
- MS5: POST disable on DISABLED row (idempotent no-op) -> 0 audit rows per LD2 / Phase 1 Q10.
- MS6: Success row's `resource_label` = the module's display label from `core.lookups` (e.g., `"Inventory Management"` not `"INVENTORY_MANAGEMENT"`).
- MS7: `request_id` correlation with X-Request-Id header.
- MF1: POST enable with TENANT JWT -> 403 PLATFORM_AUDIENCE_REQUIRED -> 1 failure row, result_type=PERMISSION_DENIED.
- MF2: POST enable with PLATFORM_ADMIN JWT lacking OVERRIDE.GLOBAL -> 403 PERMISSION_DENIED -> failure row.
- MF3: POST disable on missing row -> 404 MODULE_ACCESS_NOT_FOUND -> 0 audit rows (anchor-404 skipped per LD13).
- MF4: POST enable with invalid module_code path param -> 422 from FastAPI -> deferred per FN-AB-63 (audit row not asserted; document inline).
- MF5: Failure row's `resource_label` resolves to the module's display label even on permission denial (extractor reads `core.lookups` regardless of success).

LOAD-BEARING: MS1, MS2, MS3, MS5, MS6, MF1, MF3.

### 14. `tests/integration/test_audit_emission_org_tree.py` (NEW; ~12 tests)

Per-endpoint success + failure for org-tree.

- OS1: POST add-node success -> 1 audit row, action=`CREATE`, resource_type=`ORG_NODE`, `details.snapshot` includes `parent_org_node_name` frozen.
- OS2: PATCH edit-node rename only -> 1 row, action=`UPDATE`, `details.before.name` + `details.after.name` populated.
- OS3: PATCH edit-node reparent (parent_id change) -> 1 row, `details.before.parent_id` + `details.before.parent_org_node_name` + same in `after`. All four populated.
- OS4: PATCH edit-node archive (status change to ARCHIVED) -> 1 row, action=`UPDATE` (NOT `ARCHIVE` per LD4), `details.before.status = "ACTIVE"`, `details.after.status = "ARCHIVED"`.
- OS5: PATCH edit-node multi-field change (rename + recode in one request) -> 1 row with both diffs in before/after.
- OS6: CREATE row's `details.snapshot.path` shows the ltree path of the new node frozen.
- OS7: request_id correlation.
- OF1: POST add-node with parent_id not found -> 422 PARENT_ORG_NODE_NOT_FOUND -> 1 row, result_type=VALIDATION_FAILED.
- OF2: POST add-node with code conflict (tenant-wide uniqueness) -> 409 -> CONFLICT row.
- OF3: PATCH edit-node with empty body -> 422 EMPTY_PATCH -> VALIDATION_FAILED row.
- OF4: PATCH edit-node on nonexistent node_id -> 404 ORG_NODE_NOT_FOUND -> 0 audit rows (anchor-404 skipped).
- OF5: PATCH reparent creating a cycle -> 422 (or whichever code 6.13 raises) -> VALIDATION_FAILED row.

LOAD-BEARING: OS1, OS3, OS4, OF1, OF2.

### 15. `tests/integration/test_audit_emission_stores.py` (NEW; ~16 tests)

Per-endpoint success + failure for stores. Largest of the three given 3 endpoints + atomic-pair coverage.

- SS1: POST stores (pre-existing org_node) -> 1 row, action=`CREATE`, `details.snapshot.org_node_created_atomically = false`.
- SS2: POST stores (atomic-pair: new org_node) -> 1 row, `details.snapshot.org_node_created_atomically = true`, snapshot includes both store fields and the new org_node's frozen name.
- SS3: PATCH stores rename -> 1 row, action=`UPDATE`, `details.before.name` + `details.after.name`.
- SS4: PATCH stores org_node_id change -> 1 row, both before and after carry `org_node_id` + `org_node_name` (LD7 frozen-name contract).
- SS5: POST set-status target=OPENING -> 1 row, action=`OPEN_SOFT`, action_label=`"Soft-opened"`, standard transition payload.
- SS6: POST set-status target=ACTIVE -> 1 row, action=`ACTIVATE`.
- SS7: POST set-status target=CLOSED -> 1 row, action=`CLOSE`.
- SS8: POST set-status target=INACTIVE -> 1 row, action=`DEACTIVATE`.
- SS9: Atomic-pair POST emits ONLY 1 audit row (not 2; LD14 invariant verification).
- SS10: request_id correlation.
- SF1: POST stores duplicate store_code -> 409 -> CONFLICT row.
- SF2: POST stores with invalid org_node_id (cross-tenant or not-for-store) -> 409 ORG_NODE_NOT_FOR_STORE -> CONFLICT row.
- SF3: PATCH stores empty body -> 422 -> VALIDATION_FAILED row.
- SF4: PATCH stores nonexistent store_id -> 404 -> 0 audit rows (anchor-404 skipped).
- SF5: POST set-status INVALID transition (e.g., ACTIVE -> OPENING) -> 409 INVALID_STATE_TRANSITION -> CONFLICT row with `details.value` = current status, not target.
- SF6: POST set-status on nonexistent store_id -> 404 -> 0 audit rows (anchor-404 skipped).

LOAD-BEARING: SS1, SS2, SS5, SS9, SF1, SF5.

### 16. `tests/integration/test_audit_router_resource_type_filter.py` (NEW; ~5 tests)

GET endpoint `resource_type` filter coverage.

- RTF1: GET /audit/activities?resource_type=TENANT_USER returns only TENANT_USER rows.
- RTF2: GET /audit/activities?resource_type=STORE returns only STORE rows after 6.16.5 emission.
- RTF3: GET /audit/activities?resource_type=UNKNOWN_VALUE returns 0 rows (no 422; open vocabulary).
- RTF4: AND-composition with status filter (resource_type=ROLE + status=PERMISSION_DENIED returns only matching).
- RTF5: TENANT-caller filtering by resource_type still RLS-scoped. Filtering by `resource_type=TENANT_USER` returns own-tenant rows. Filtering by `resource_type=ROLE` returns 0 rows even if the platform table has matching rows (TENANT caller never reaches platform table per 6.16.3 LD13; RLS does its job).

LOAD-BEARING: RTF1, RTF3, RTF4.

### 17. `docs/architecture_audit_logs.md` (MODIFY)

Apply Appendix A: four edits as framed there.

### 18. `BUILD_PLAN.md` (MODIFY)

Apply Appendix A: 6.16.5 status flip + 6.16 root closure note.

### 19. `CLAUDE.md` (MODIFY)

Add one-line pointer to step doc + flip FN-AB-65 and FN-AB-66 to CLOSED with resolution dates. Composed at apply time per A6 lean convention.

### 20. `docs/implementation-steps/step-6_16_5-audit-emission-module-access-org-tree-stores-2026-05-21.md` (NEW)

Per A6 step doc convention (Plan + Mental Model + Retro). Brief.

### 21. `prompts/step-6_16_5-impl-2026-05-21.md` (NEW; this prompt)

Bundled into the commit.

### 22. `docs/endpoints/openapi.json` (REGEN)

GET /audit/activities gains the `resource_type` query parameter. Regenerate post-impl.

## Test catalogue summary

| File | Status | Test count | Load-bearing |
|---|---|---|---|
| `tests/unit/test_audit_emit.py` | MODIFY | +2 (AE10-AE11) | AE11 |
| `tests/integration/test_audit_emission_module_access.py` | NEW | ~12 | MS1, MS2, MS3, MS5, MS6, MF1, MF3 |
| `tests/integration/test_audit_emission_org_tree.py` | NEW | ~12 | OS1, OS3, OS4, OF1, OF2 |
| `tests/integration/test_audit_emission_stores.py` | NEW | ~16 | SS1, SS2, SS5, SS9, SF1, SF5 |
| `tests/integration/test_audit_router_resource_type_filter.py` | NEW | ~5 | RTF1, RTF3, RTF4 |
| **Total** | | **~47** | **~23** |

Existing test files (`test_audit_emission_*` from 6.16.2 and 6.16.4) unchanged.

## Per-resource regression checkpoint

Baselines captured at pre-flight Check #17. Expected post-step counts:

| Resource | Test file | Expected delta |
|---|---|---|
| tenants writes | `test_tenants_writes_router.py` | +0 |
| tenant-users writes | `test_tenant_users_writes_router.py` | +0 |
| roles writes | `test_rbac_writes_router.py` | +0 |
| module-access writes | `test_module_access_writes_router.py` | +0 |
| org-tree writes | `test_org_tree_writes_router.py` | +0 |
| stores writes | `test_stores_writes_router.py` | +0 |
| stores set-status | `test_stores_set_status_router.py` | +0 |
| audit reads | `test_audit_router.py` | +0 |
| audit reads repo | `test_audit_logs_repo.py` | +0 |
| audit emission tenants (success) | `test_audit_emission_tenants.py` | +0 |
| audit emission tenants (failures) | `test_audit_emission_failures.py` | +0 |
| audit emission tenant-users (success) | `test_audit_emission_tenant_users.py` | +0 |
| audit emission tenant-users (failures) | `test_audit_emission_failures_tenant_users.py` | +0 |
| audit emission roles | `test_audit_emission_roles.py` | +0 |
| audit emit unit | `test_audit_emit.py` | +2 |
| audit emission module-access (NEW) | `test_audit_emission_module_access.py` | +~12 |
| audit emission org-tree (NEW) | `test_audit_emission_org_tree.py` | +~12 |
| audit emission stores (NEW) | `test_audit_emission_stores.py` | +~16 |
| audit GET resource_type filter (NEW) | `test_audit_router_resource_type_filter.py` | +~5 |

Total expected: 824 -> ~871 (+~47). Any unexpected delta on existing files: stop, surface, do not auto-fix.

## Testing and regression discipline

**Load-bearing tests** (must pass; cite by ID in the report):

AE11, MS1, MS2, MS3, MS6, MF1, MF3, OS1, OS3, OS4, OF1, OF2, SS1, SS2, SS5, SS9, SF1, SF5, RTF1, RTF3, RTF4.

**Deliberately NOT added in this commit:**

- Performance/EXPLAIN ANALYZE on the new emission SELECTs (eager name + lookup resolution): v0 scale sub-millisecond; deferred to Scale Considerations triggers.
- Frontend rendering integration for the new resource_type filter UI.
- Cross-resource emission sanity (single integration test exercising 3+ resource families): redundant given per-resource coverage.
- Module-access label resolution against a missing `core.lookups` row: defensive code path; deferred unless impl surfaces it as a concern.

**Regression risk surface:**

- The LD12 per-route extractor mapping rewrite is the largest structural change. It touches the failure-path handler in `main.py` that 6.16.4 just shipped. Existing 6.16.4 emission tests (AS, AF, RS, RF series; 27 tests) must still pass with the new shape.
- AUDITED_ROUTES dict ordering: append-only; do not reorder existing entries.
- New eager-lookup queries inside repo methods (module display label, org_node name, parent_org_node_name) are additional SELECTs inside the data transactions. At v0 scale, additive latency is sub-millisecond.
- Audit table FK ON DELETE RESTRICT: the make_tenant teardown extension from 6.16.4 covers all three new resource families (LD19 verification).

## Surface-and-stop scenarios

Stop and report; do not work around silently if:

0. **Any file or directory referenced in this prompt does not exist at HEAD.** Surface and pause.
1. Pre-flight Check #2 baseline pytest count differs from 824 by more than 5 in either direction. Surface; lower means tests broke since 6.16.4 closure, higher means intervening work landed that wasn't expected.
2. Pre-flight Check #3 shows any of the 7 new routes already in AUDITED_ROUTES. STOP; suggests prior partial work.
3. Pre-flight Check #5 shows the failure-path handler shape is not the 6.16.4 fallthrough — different shape suggests intermediate work; surface.
4. Pre-flight Check #7 fails to find any `core.lookups` query pattern. Surface; the module display label resolution needs an existing precedent or a new helper.
5. Pre-flight Check #8 cannot identify the atomic-pair detection mechanism on `StoresRepo.create`. Surface; LD6's `org_node_created_atomically` flag needs a reliable boolean source.
6. Pre-flight Check #9 shows the make_tenant teardown extension is missing or different shape from 6.16.4 iteration 3. Surface; LD19 assumed it's in place.
7. LD12 per-route extractor mapping has more than two reasonable shapes (CC surfaces a third). Surface for operator decision.
8. Atomic-pair stores POST emits more than 1 audit row (violates LD14). STOP; emission code has a bug.
9. Module-access disable on missing row triggers an audit emission (contradicts LD13 + anchor-404 precedent). STOP; handler integration is wrong.
10. Any of the existing 6.16.4 emission tests (AS / AF / RS / RF series) fails post-extractor-rewrite. STOP; the rewrite broke back-compat.

## Report shape

Format CC produces at Phase 5 exit, before staging. STOP after producing; wait for operator authorisation before staging.

Two parts: **status summary** (counts, decisions, findings) and **doc edit previews** (full proposed prose for each documentation file).

### Part 1: Status summary

```
Step 6.16.5, DONE-LOCAL (pending operator authorisation)

Files changed: <N> files, +<add>/-<del> lines
  Modified:
    src/admin_backend/audit/emit.py
    src/admin_backend/main.py
    src/admin_backend/repositories/modules_access.py
    src/admin_backend/repositories/org_nodes.py
    src/admin_backend/repositories/stores.py
    src/admin_backend/repositories/audit_logs.py
    src/admin_backend/routers/v1/module_access.py
    src/admin_backend/routers/v1/org_tree.py
    src/admin_backend/routers/v1/stores.py
    src/admin_backend/routers/v1/audit.py
    src/admin_backend/schemas/audit_log.py (if surfaced at Check #10)
    tests/unit/test_audit_emit.py
    docs/architecture_audit_logs.md
    docs/endpoints/openapi.json
    BUILD_PLAN.md
    CLAUDE.md
  New:
    tests/integration/test_audit_emission_module_access.py
    tests/integration/test_audit_emission_org_tree.py
    tests/integration/test_audit_emission_stores.py
    tests/integration/test_audit_router_resource_type_filter.py
    docs/implementation-steps/step-6_16_5-audit-emission-module-access-org-tree-stores-2026-05-21.md
    prompts/step-6_16_5-impl-2026-05-21.md

Tests:
  pytest full suite: 824 -> <824 + ~47>
  mypy strict: clean on <count> src files
  check_setup: 36/36 (unchanged)

Per-resource regression checkpoint:
  <table with actual pre/post counts; all existing files unchanged>

Load-bearing tests verified by ID:
  AE11, MS1, MS2, MS3, MS5, MS6, MF1, MF3, OS1, OS3, OS4, OF1, OF2,
  SS1, SS2, SS5, SS9, SF1, SF5, RTF1, RTF3, RTF4

Locked-decision honour record:
  LD1 ... LD20, each Honoured / Adjusted-trivial / Stopped-for-confirmation

Surface-and-stop findings: <0 to N items>

Codebase observations beyond step scope: <0 to N items>

FN-AB closures:
  FN-AB-65 closed: 6.16 series shipped; all 16 v0 write endpoints emit audit rows
  FN-AB-66 closed: per-route extractor mapping shipped in this commit
```

### Part 2: Doc edit previews

CC produces the **full proposed prose** for each doc edit BEFORE staging. Operator reviews each block.

**Block 2a: CLAUDE.md proposed additions**

- Step doc pointer line per A6 lean convention (mirror the 6.16.4 capsule shape).
- FN-AB-65 entry flipped to CLOSED with resolution date.
- FN-AB-66 entry flipped to CLOSED with resolution date.

**Block 2b: BUILD_PLAN.md proposed edits**

- Step 6.16.5 status flip (TODO -> DONE-LOCAL) with as-shipped block.
- Step 6.16 root entry closure note: "All 16 v0 write endpoints emit audit rows; series complete."

**Block 2c: docs/architecture_audit_logs.md proposed edits**

- Emission contract section: per-route extractor mapping paragraph (FN-AB-66 closure framing).
- Read contract section: resource_type filter mention.
- Sub-step plan table: 6.16.5 row flipped to DONE-LOCAL; closing note.
- New resource_type vocabulary table.

**Block 2d: docs/implementation-steps/step-6_16_5-...md (NEW)**

Per A6 step doc shape: Mental Model + Implementation Plan + Retro.

## Phase 5 entry: pre-flight Report-Pause-Authorise (A9)

CC produces the full pre-flight report per the format above. STOP. Operator reviews. Wait for operator authorisation before edits.

## Phase 5 exit: Report-Pause-Authorise before commit (A7)

After implementation + verification:

1. Produce the report per the Report shape section. STOP. Wait for operator authorisation.

2. After authorisation, stage and commit:

```bash
git add \
  src/admin_backend/audit/emit.py \
  src/admin_backend/main.py \
  src/admin_backend/repositories/modules_access.py \
  src/admin_backend/repositories/org_nodes.py \
  src/admin_backend/repositories/stores.py \
  src/admin_backend/repositories/audit_logs.py \
  src/admin_backend/routers/v1/module_access.py \
  src/admin_backend/routers/v1/org_tree.py \
  src/admin_backend/routers/v1/stores.py \
  src/admin_backend/routers/v1/audit.py \
  tests/unit/test_audit_emit.py \
  tests/integration/test_audit_emission_module_access.py \
  tests/integration/test_audit_emission_org_tree.py \
  tests/integration/test_audit_emission_stores.py \
  tests/integration/test_audit_router_resource_type_filter.py \
  docs/architecture_audit_logs.md \
  docs/endpoints/openapi.json \
  BUILD_PLAN.md \
  CLAUDE.md \
  docs/implementation-steps/step-6_16_5-audit-emission-module-access-org-tree-stores-2026-05-21.md \
  prompts/step-6_16_5-impl-2026-05-21.md

# Conditionally also: src/admin_backend/schemas/audit_log.py (only if pre-flight Check #10 surfaced schema change needed)

git status
git diff --cached --stat

git commit \
  -m "Step 6.16.5: audit emission for module-access + org-tree + stores endpoints + GET resource_type filter + tests + docs + retro" \
  -m "Wires synchronous audit emission into the final 7 v0 write endpoints across 3 resource families: module-access (enable, disable), org-tree (POST add-node, PATCH edit-node), stores (POST, PATCH, set-status). AUDITED_ROUTES extended with 7 new entries; all route to tenant_activity_audit_logs (tenant_id always known). Closes the 6.16 series: all 16 v0 write endpoints now emit audit rows." \
  -m "Stores set-status dispatches on target_status to pick 1 of 4 per-target action codes per Phase 1 Q3 (OPEN_SOFT, ACTIVATE, CLOSE, DEACTIVATE; ACTIVATE reused from tenants). Org-tree edit-node always uses UPDATE action code regardless of fields changed per LD4 (action codes are per-endpoint-shape, not per-field-change). Module-access ENABLE/DISABLE emit single action codes with before/after status distinguishing first-time vs re-activation per LD2. Atomic-pair stores POST emits one audit row per LD6 with org_node_created_atomically flag in snapshot." \
  -m "FN-AB-66 closure: per-route extractor mapping replaces the 6.16.4 minimal fallthrough. Each resource_type declares its identifying path-param key and resource_label lookup source. Module-access uses core.lookups for display label (Inventory Management not INVENTORY_MANAGEMENT) — third lookup table on top of core.tenants, core.tenant_users, core.roles, core.org_nodes, core.stores." \
  -m "GET /api/v1/audit/activities gains resource_type query param (optional, open string vocabulary, AND-composed with existing filters, applied to both UNION branches). OpenAPI regenerated. Frontend filter UI deferred." \
  -m "Tests: 824 -> <824 + ~47>. mypy strict clean. check_setup 36/36 unchanged. No DDL changes; no migration; no smoke / test_endpoint script changes; no permission catalogue change; no new permission tuples. FN-AB-65 closes (series acceptance criterion met). FN-AB-66 closes (extractor mapping shipped). FN-AB-67 stays open (actor enrichment; trigger criteria documented)."

git log -1 --stat
# Do NOT push.
```

## Coordination

- **Unblocks**: nothing in 6.x; 6.16 series closes. Frontend integration of the resource_type filter UI tracked separately.
- **Cloud SQL**: no changes.
- **Cloud deploy**: deferred per Phase 5.5; batches with 6.16.4 and any other pending DONE-LOCAL steps at next deploy cycle.

## Non-goals (explicit scope-out)

- No new write endpoints.
- No permission catalogue change.
- No smoke or test_endpoint script changes.
- No Alembic migration.
- No DDL changes.
- No frontend integration for the resource_type filter UI.
- No async outbox pattern.
- No JSONB GIN / trigram / actor BTREE indexes.
- No partition tables.
- No FN-AB-58 promotion of `_actor_type_from_auth`.
- No FN-AB-67 actor enrichment (full_name + role snapshot).
- No downstream impact summary on org-tree edits.
- No orphan-detection inline flag on store PATCH.
- No body-snapshot on failure rows.

---

## Appendix A: framing prose for doc edits bundled in this commit (LD20)

Per WORKFLOW.md Architecture doc updates discipline: Phase 2 close drafts the framing prose; Phase 4 folds it into the impl prompt as Appendix A; Phase 5 applies it. The framing prose below is the source of truth for what each doc says after the edits.

### Edit 1: FN-AB-66 closure in Emission contract section

**Where**: `docs/architecture_audit_logs.md` Emission contract section, after the existing two-rule transaction semantics sub-section.

**Framing**: 6.16.4 introduced a minimal multi-key path-param fallthrough in the failure-path handler (`tenant_id` -> `user_id` -> `role_id`) and a `resource_type`-dispatched resource_label lookup (TENANT -> tenants, TENANT_USER -> tenant_users, ROLE -> roles). 6.16.5 adds three more resource families with three new lookup tables (ORG_NODE -> org_nodes; STORE -> stores; MODULE_ACCESS -> core.lookups keyed by module_code). The minimal fallthrough scales to ~5 paths before it becomes cluttered.

The 6.16.5 work promotes the pattern to a per-route extractor mapping. Each audited route declares (via AUDITED_ROUTES extension or a sibling dict) the function that extracts `(resource_id, tenant_id, resource_label)` from a Request + exception. The failure-path handler consults the mapping after resolving the route template, calls the extractor, and proceeds to emission. Success-path emission continues to provide these values directly from the repo (no extractor consultation needed on the happy path).

The convention's contract: a new audited resource_type adds (a) one AUDITED_ROUTES entry per affected route and (b) one extractor function. The extractor encapsulates path-param key, lookup source table, and tenant_id resolution for that resource_type.

CC composes the exact prose at apply time.

### Edit 2: resource_type filter mention in Read contract section

**Where**: `docs/architecture_audit_logs.md` Read contract section.

**Framing**: the GET list endpoint accepts `resource_type` as an optional query parameter (string, open vocabulary). Filters rows by the audit row's `resource_type` column. AND-composes with existing filters (`tenant_id`, `scope`, `status`, `from`, `to`, `search`). Both UNION branches (tenant + platform) receive the filter. Unknown values produce 0 rows naturally (no 422).

One paragraph addition. CC composes the exact insertion site at apply time.

### Edit 3: sub-step plan table closure

**Where**: `docs/architecture_audit_logs.md` Sub-step plan section.

**Framing**: flip 6.16.5 row to DONE-LOCAL. Add a closing note below the table:

> **6.16 series complete.** All 16 v0 write endpoints across 6 resource families (tenants 4 + tenant-users 4 + module-access 2 + org-tree 2 + stores 3 + roles 1) emit synchronous audit rows on success and failure paths. The audit subsystem reads via GET /api/v1/audit/activities (list + detail) per 6.16.3. Frontend integration follows.

CC composes the exact insertion site at apply time.

### Edit 4: resource_type vocabulary table

**Where**: `docs/architecture_audit_logs.md` Schema section, near the `resource_type` column description.

**Framing**: add a small reference table:

| `resource_type` value | Source step | Underlying table |
|---|---|---|
| `TENANT` | 6.16.2 | `core.tenants` |
| `TENANT_USER` | 6.16.4 | `core.tenant_users` |
| `ROLE` | 6.16.4 | `core.roles` |
| `MODULE_ACCESS` | 6.16.5 | `core.tenant_module_access` |
| `ORG_NODE` | 6.16.5 | `core.org_nodes` |
| `STORE` | 6.16.5 | `core.stores` |

Note convention: UPPERCASE_SINGULAR, table-name-derived with brevity preferred when prefix is redundant (e.g., MODULE_ACCESS not TENANT_MODULE_ACCESS).

CC composes the exact insertion site at apply time.

---

End of impl prompt.
