# Step 6.15: Tenant Module Access write endpoints (toggle on existing tenants)

## Standing discipline

- Read CLAUDE.md, BUILD_PLAN.md, docs/architecture.md, docs/architecture_RBAC.md, and docs/schema/current_schema.sql end-to-end before any code change. Surface-and-stop on any contradiction between this prompt and those docs; the docs usually win.
- No code sketches or method bodies inline below. Behaviour specs plus acceptance tests are the contract. Implement against live conventions.
- One commit for the whole step. Report and authorise once.
- No em-dashes in any document text this step adds (CLAUDE.md standing rule).
- No DDL changes, no Alembic migration, no seed Excel changes.

## Context

Step 6.15 ships two platform-only write endpoints that toggle `core.tenant_module_access.status` between `ENABLED` and `DISABLED` on existing rows, with one upsert seam on the enable path. The endpoints are the third write surface in v0 after `tenants` (6.11.2) and `tenant_users` (6.10.1). Re-scoped at 6.11 prep: the create-with-modules bundle for new tenants already shipped inside `POST /api/v1/tenants` at 6.11.2; 6.15 is the toggle path for existing tenants only.

Stage 1: HEAD at 08ff068; pytest 416 passing; check_setup 36/36; cloud deployed at v0.1.13 (Step 6.10.1); A6 retro-fold-in active.

## Pre-flight items (run before any code change; surface-and-stop on miss)

1. Run `./scripts/check_setup.sh`. Expect 36/36 PASS. Stop on any fail.
2. Read CLAUDE.md, BUILD_PLAN.md, docs/architecture.md, docs/architecture_RBAC.md end-to-end. Surface-and-stop on contradictions.
3. DDL verification, two checks:
   - **3a. Snapshot check.** Read `docs/schema/current_schema.sql` and confirm `core.tenant_module_access` matches the DDL facts in this prompt (column types, NULL flags, CHECK names, FK destinations, RLS policy text, trigger name).
   - **3b. Live-DB final gate.** Query the live local DB and confirm it matches the same DDL facts. Use any of:
     - `psql -c "\d core.tenant_module_access"` via the local DB connection string
     - SQL against `information_schema.columns`, `information_schema.table_constraints`, `pg_constraint` (for CHECK text), and `pg_policies` (for RLS clause text)
   - **3c. Migration history sanity check.** Confirm `core.alembic_version.version_num` equals the head revision in `migrations/versions/` (`alembic heads` matches the row). Then grep `migrations/versions/*.py` for any file containing `tenant_module_access` whose timestamp is newer than `docs/schema/current_schema.sql`. If any are found, the snapshot is stale: 3b's live-DB output is the source of truth; surface the divergence so the operator can regenerate the snapshot in a follow-on commit if warranted.
   - If 3a and 3b disagree, the snapshot is stale: trust 3b, surface 3a's drift for the operator's awareness. If 3b disagrees with the prompt's DDL facts, surface and stop; the prompt itself needs updating before code lands.
4. Grep-verify these exist before NEW-flagging anything:
   - `ModulesAccessRepo` class in `src/admin_backend/repositories/modules_access.py` (Step 6.7; REUSE, do not recreate)
   - `Python enum mirroring core.module_code_enum`: locate the canonical Python class (likely `ModuleCode` in `src/admin_backend/models/` or similar). Confirm its values match the 6 PG enum values verbatim. Surface-and-stop if absent or named differently than expected; select the existing canonical class, do not duplicate.
   - `Python enum mirroring core.module_access_status_enum` (values `ENABLED`, `DISABLED`): if it exists, REUSE; if absent, CREATE in `src/admin_backend/models/tenant_module_access.py` alongside the ORM model and re-export via `models/__init__.py` per project convention. Required for `ModuleAccessRead.status: ModuleAccessStatus` and for the repo's status-string CAST sites.
   - `get_tenant_anchor` in `src/admin_backend/auth/anchor_deps.py` (Step 6.9.3.2; REUSE)
   - `require()` factory accepts `audience="PLATFORM"` keyword (Step 6.11.1; REUSE)
   - `PlatformAudienceRequiredError`, `PermissionDeniedError`, `TenantNotFoundError` in `errors.py` (REUSE)
   - `write_req` shim in `scripts/test_endpoints.sh` (Step 6.11.2; REUSE)
5. Per-resource regression baseline: capture pytest counts per existing router test file. The commit must leave them unchanged at baseline.

## Locked decisions

| # | Decision | Owner artifact |
|---|----------|----------------|
| LD1 | URL shape: writes live under `/api/v1/module-access/` alongside the existing reads. Format `POST /api/v1/module-access/{tenant_id}/{module_code}/enable` and `.../disable`. | `routers/v1/modules_access.py` |
| LD2 | Audience: PLATFORM-only. Layer 1 rejects TENANT JWTs with `PLATFORM_AUDIENCE_REQUIRED` before the permission check. | `Depends(require(..., audience="PLATFORM", ...))` |
| LD3 | Permission tuple: `ADMIN.TENANTS.OVERRIDE.GLOBAL` (SUPER_ADMIN only per Phase 3 seed). Reuses the tuple already wired for tenant suspend/activate; same privilege boundary. | `require(ModuleCode.ADMIN, PermissionResource.TENANTS, PermissionAction.OVERRIDE, PermissionScope.GLOBAL, audience="PLATFORM", anchor_dep=get_tenant_anchor)` |
| LD4 | Transition matrix: idempotent-200 on no-op cells. `enable` on ENABLED is 200 with no row mutation; `disable` on DISABLED is 200 with no row mutation; `disable` on missing is 404 `MODULE_ACCESS_NOT_FOUND`. No 409 `INVALID_STATE_TRANSITION` on this endpoint pair. | `ModulesAccessRepo.enable / .disable`; test cells C1 to C6 |
| LD5 | `enabled_at` and `enabled_by_user_id` are overwritten on every DISABLED to ENABLED flip and set on initial INSERT. Treated as "current ENABLED stint began at" markers, symmetric with `disabled_at` as "most-recent DISABLE event". | `ModulesAccessRepo.enable` UPDATE path; test RT3 |
| LD6 | HTTP status: 200 OK on every successful response (create, transition, no-op). 201 is not used; the verb-suffix POST is a controller endpoint, not a resource-create endpoint. | Router return spec; OpenAPI summaries |
| LD7 | `module_code` path param binds to the canonical `ModuleCode` Python enum. Unknown values produce 422 at FastAPI's path-validation layer, before the handler runs. | Router signature; test V1 |
| LD8 | Upsert race control: `SELECT ... FOR UPDATE` on `(tenant_id, module)` inside the request transaction. On UniqueViolation during the INSERT path (concurrent enable-on-missing race), catch, re-run SELECT FOR UPDATE, take the UPDATE branch. Retry-once is sufficient (the second SELECT sees the committed row). | `ModulesAccessRepo.enable`; test RT2 |

## DDL facts (verified against current_schema.sql at 2026-05-15)

### core.tenant_module_access

- PK: `id UUID DEFAULT core.uuidv7() NOT NULL`
- UNIQUE `uq_tenant_module_access_tenant_module(tenant_id, module)` , race-control arbiter for enable upsert
- Enum columns:
  - `module core.module_code_enum NOT NULL` with 6 values: `ROOS, PRICING_OS, PERISHABLES_ASSISTANT, PROMOTIONS_ASSISTANT, GOAL_CONSOLE, ADMIN`
  - `status core.module_access_status_enum NOT NULL` with 2 values: `ENABLED, DISABLED`
- `enabled_at TIMESTAMPTZ NOT NULL`, `enabled_by_user_id UUID NOT NULL`: always populated regardless of current status
- `disabled_at TIMESTAMPTZ`, `disabled_by_user_id UUID`: both nullable, governed by:
  - `ck_tenant_module_access_disabled_pair`: both NULL or both NOT NULL
  - `ck_tenant_module_access_status_consistency`: `status='ENABLED' AND disabled_at IS NULL` OR `status='DISABLED' AND disabled_at IS NOT NULL`. Pair flips atomically with status.
- Standard audit pair: `created_at` / `created_by_user_id` / `updated_at` / `updated_by_user_id`, all NOT NULL. Timestamps default `now()`.
- Pattern (a) audit-actors per D-13: all four `*_by_user_id` columns are typed FKs to `core.platform_users(id)`, ON UPDATE RESTRICT ON DELETE RESTRICT. No `*_by_user_type` column. PLATFORM-only audience (LD2) makes this structurally satisfiable; no FN-AB-37 blocker.
- FK `tenant_id` to `core.tenants(id)`, ON UPDATE RESTRICT ON DELETE RESTRICT.
- Trigger `tg_tenant_module_access_set_updated_at`: BEFORE UPDATE FOR EACH ROW, executes `core.set_updated_at_timestamp()`. Sets `updated_at` only. Repo must set `updated_by_user_id` and `enabled_by_user_id` / `disabled_by_user_id` explicitly.
- Index `ix_tenant_module_access_tenant_id`
- RLS: ENABLE + FORCE; policy `tenant_module_access_tenant_isolation` USING and WITH CHECK both:
  - `tenant_id = NULLIF(current_setting('app.tenant_id', true), '')::uuid OR current_setting('app.user_type', true) = 'PLATFORM'`
  - D-29 unconditional OR-branch, D-27 NULLIF wrapper

## Transition contract (six cells)

| Current row state | POST .../enable | POST .../disable |
|---|---|---|
| missing | INSERT `status=ENABLED`, `enabled_at=now`, `enabled_by_user_id=auth.user_id`, `created_by_user_id=auth.user_id`, `updated_by_user_id=auth.user_id`. Return 200 + `ModuleAccessRead`. | 404 `MODULE_ACCESS_NOT_FOUND` |
| `status=DISABLED` | UPDATE `status=ENABLED`, `enabled_at=now` (overwrite, per LD5), `enabled_by_user_id=auth.user_id` (overwrite), `disabled_at=NULL`, `disabled_by_user_id=NULL`, `updated_by_user_id=auth.user_id`. Return 200 + `ModuleAccessRead`. | 200 + existing row no-op (no UPDATE issued) |
| `status=ENABLED` | 200 + existing row no-op (no UPDATE issued) | UPDATE `status=DISABLED`, `disabled_at=now`, `disabled_by_user_id=auth.user_id`, `updated_by_user_id=auth.user_id`. Return 200 + `ModuleAccessRead`. |

`enabled_at` is preserved on the `disable` flip (carries the "current ENABLED stint began at" forward into the disabled state as historical record, per LD5's reading).

## File-by-file change list (single commit)

### NEW files (none , every change lands in existing files)

### MODIFY

| File | Change |
|---|---|
| `src/admin_backend/repositories/modules_access.py` | Add `enable`, `disable`, and private `_transition` helper methods on `ModulesAccessRepo`. Add `TransitionResult` enum local to this module (mirror of `repositories/tenants.py`'s pattern; do not import the tenants one). Methods use raw `text()` SQL with `f"{schema}.tenant_module_access"` interpolation per the schema-qualification convention. SELECT-then-conditional-INSERT-or-UPDATE under SELECT FOR UPDATE; UniqueViolation retry per LD8. `session.expire_all()` after UPDATE so any post-write read returns fresh data. |
| `src/admin_backend/schemas/modules_access.py` | Add `ModuleAccessRead` Pydantic schema with `ConfigDict(from_attributes=True, extra="forbid")`. Fields: `id: UUID`, `tenant_id: UUID`, `module: ModuleCode`, `status: ModuleAccessStatus`, `enabled_at: datetime`, `disabled_at: datetime \| None`, `created_at: datetime`, `updated_at: datetime`. Audit-actor IDs hidden per H1 convention. Re-export from `schemas/__init__.py`. |
| `src/admin_backend/errors.py` | Add `ModuleAccessNotFoundError(ClientError)`: `http_status = 404`, `code = "MODULE_ACCESS_NOT_FOUND"`, `public_message = "Module access not found for the requested tenant and module."` Structured context fields (`tenant_id`, `module_code`) attach via `**context` and reach logs only (Q7 envelope per Step 6.10.1). |
| `src/admin_backend/routers/v1/modules_access.py` | Add two handler functions: `enable_module_for_tenant(tenant_id: UUID, module_code: ModuleCode, ...)` and `disable_module_for_tenant(...)`. Both gated `Depends(require(ModuleCode.ADMIN, PermissionResource.TENANTS, PermissionAction.OVERRIDE, PermissionScope.GLOBAL, audience="PLATFORM", anchor_dep=get_tenant_anchor))`. Handlers call repo, map `TransitionResult.NOT_FOUND` (disable-on-missing path) to `ModuleAccessNotFoundError`, return `ModuleAccessRead`. Status code 200 on every success per LD6. |
| `tests/integration/test_module_access_writes_router.py` (NEW file under integration/) | 14 router tests per the catalogue below. |
| `tests/integration/test_module_access_repo_writes.py` (NEW file under integration/) | 4 repo tests per the catalogue below. |
| `tests/unit/test_module_access_schemas.py` (NEW file under unit/) | 2 schema tests per the catalogue below. |
| `tests/unit/test_module_access_errors.py` (NEW file under unit/) | 1 error-envelope test per the catalogue below. |
| `tests/integration/test_gate_discipline.py` | Extend `test_gate_discipline_platform_only_writes_declare_audience` from 4 tuples to 6 tuples (the two new `/module-access/.../enable\|disable` paths added). |
| `scripts/smoke_curl.sh` | +6 entries (WHAT'S CHECKED 32 to 38). See Smoke catalogue below. |
| `scripts/test_endpoints.sh` | New Phase 4d block for module-access writes. PLATFORM happy paths plus TENANT audience-deny. Use `write_req` shim from 6.11.2 to thread JWT and body together. |
| `scripts/test_endpoints_cloud.sh` | Mirror the test_endpoints.sh addition. |
| `docs/endpoints/module-access.md` | Append two new operations in the canonical 8-section format alongside the existing GET pair. |
| `docs/endpoints/openapi.json` | REGEN. Both new paths appear with their request, response, and error schemas. |
| `BUILD_PLAN.md` | Flip Step 6.15 status TODO to DONE-LOCAL. Bundle a one-line correction to the Step 6.7 "Scope out" wording: replace "Disabling instantly revokes all related role permissions" with a note that the cascade is structural (the `has_permission()` JOIN on `tenant_module_access.status='ENABLED'` blocks access on the next request; no imperative revocation pass; re-enable restores access automatically per D-24's identity-only JWT). |
| `CLAUDE.md` | Add FN-AB-NN (next available number) for cross-resource transition-matrix asymmetry vs tenants (modules idempotent-200; tenants 409 INVALID_STATE_TRANSITION). Resolution criterion: revisit at Step 6.16 when audit-log emission surfaces the billing-mutation audit-trail differences concretely. Add FN-AB-NN (next available number) for module-access schema evolution under billing/payments. Resolution criterion: a Stage 3 or post-v0 billing-integration design step. Add one cookbook entry under "Current state" matching prior step format. No new D-NN decision (URL convention pattern still 1-instance; codify when role-assignment writes at 6.14 give the second example). |
| Architecture doc | Apply Appendix A verbatim to `docs/architecture_RBAC.md` per Appendix A's insertion marker. |
| Step doc | NEW `step-6_15-impl-2026-05-15.md` at repo root with Plan, Mental Model (from this prompt's Context plus DDL facts), and Retro sections populated per A6. |
| `prompts/step-6_15-impl-2026-05-15.md` | NEW. This prompt file, committed. |

## Test catalogue

### Router tests (`tests/integration/test_module_access_writes_router.py`)

Fixture order required (LIFO teardown): `..., make_platform_user, cleanup_module_access_router, ...`. The cleanup fixture tracks `tenant_module_access` row IDs and DELETEs them before `make_platform_user`'s teardown so FK refs to platform_users clear correctly. Mirror the cleanup-fixture pattern documented for 6.11.2's `test_tenants_writes_router.py`.

| ID | Verb | Setup | Assertion | Load-bearing? |
|---|---|---|---|---|
| C1 | enable | row missing for (tenant, module) | 200; new row created with `status=ENABLED`, `enabled_at` recent, `enabled_by_user_id=actor`, all 4 audit columns populated; `disabled_*` NULL | yes (upsert path) |
| C2 | enable | row exists, `status=DISABLED` | 200; row updated to `status=ENABLED`; `enabled_at` overwritten to recent (compared to pre-state `enabled_at` captured before request); `enabled_by_user_id` overwritten to actor; `disabled_*` cleared to NULL | yes (LD5 overwrite) |
| C3 | enable | row exists, `status=ENABLED` | 200; row UNCHANGED (id, all timestamps, all *_by_user_id pre and post equal); no UPDATE issued | yes (idempotent no-op) |
| C4 | disable | row missing | 404 `MODULE_ACCESS_NOT_FOUND` | yes |
| C5 | disable | row exists, `status=ENABLED` | 200; row updated to `status=DISABLED`; `disabled_at` recent; `disabled_by_user_id=actor`; `enabled_at` and `enabled_by_user_id` UNCHANGED from pre-state | yes |
| C6 | disable | row exists, `status=DISABLED` | 200; row UNCHANGED; no UPDATE | yes (idempotent no-op) |
| P1 | enable | TENANT JWT | 403 `PLATFORM_AUDIENCE_REQUIRED` (Layer 1) | yes (cross-tenant write disaster guard) |
| P2 | disable | TENANT JWT | 403 `PLATFORM_AUDIENCE_REQUIRED` (Layer 1) | yes |
| P3 | enable | PLATFORM_ADMIN JWT (holds CONFIGURE.GLOBAL not OVERRIDE.GLOBAL) | 403 `PERMISSION_DENIED` (Layer 2) | yes (catches OVERRIDE-to-CONFIGURE catalogue regression; mirrors 6.11.2 S6) |
| P4 | enable | SUPER_ADMIN JWT, `tenant_id` does not exist | 404 `TENANT_NOT_FOUND` from anchor dep | yes (anchor-miss order; mirrors 6.9.3.2 T_RET_3) |
| V1 | enable | path param `module_code=FAKEMOD` | 422 from FastAPI path-validation; response body includes the valid enum values | yes (LD7 binding contract) |
| AUD-1 | enable | TENANT JWT, PLATFORM_ADMIN's caller principal would also fail Layer 2 | response code = `PLATFORM_AUDIENCE_REQUIRED`, NOT `PERMISSION_DENIED` (Layer 1 fires first, ahead of has_permission); patched `has_permission` AsyncMock asserted `call_count == 0` | yes (gate ordering invariant; mirror of 6.11.2's AUD-2) |
| R1 | enable | concurrent SUPER_ADMIN happy-path repeat (sequential, not parallel) | second call sees `status=ENABLED` from first; returns 200 no-op (regression: confirms cell C3 holds after C1 wrote the row in the same test session) | no (regression) |
| R2 | disable | sequential: enable, then disable, then enable | final state ENABLED with `enabled_at` overwritten between the two enables; one `disabled_at` write between them (now NULL in final state) | no (regression on overwrite ordering) |

### Repo tests (`tests/integration/test_module_access_repo_writes.py`)

| ID | What | Load-bearing? |
|---|---|---|
| RT1 | `enable()` and `disable()` issue SELECT FOR UPDATE on `(tenant_id, module)` ahead of the conditional INSERT/UPDATE (verify via session.in_transaction() and EXPLAIN ANALYZE) | yes (race control) |
| RT2 | Upsert race: pre-INSERT a row in a separate session committed mid-test; `enable()` catches UniqueViolation, retries with SELECT FOR UPDATE, takes UPDATE branch, returns the existing row's id | yes (LD8) |
| RT3 | DISABLED to ENABLED flip overwrites `enabled_at` and `enabled_by_user_id` (compared row state before and after) | yes (LD5) |
| RT4 | No-op path (enable on ENABLED, disable on DISABLED) returns the existing row without issuing any UPDATE: verify by checking `updated_at` is unchanged | yes |

### Schema tests (`tests/unit/test_module_access_schemas.py`)

| ID | What |
|---|---|
| S1 | `ModuleAccessRead` serialises `module` and `status` as canonical string enum values |
| S2 | `ModuleAccessRead` rejects audit-actor ID fields via `extra="forbid"` (the schema does not include them; verify Pydantic raises on construction with them) |

### Error test (`tests/unit/test_module_access_errors.py`)

| ID | What |
|---|---|
| E1 | `ModuleAccessNotFoundError` raises with `http_status=404`, `code="MODULE_ACCESS_NOT_FOUND"`; envelope `details=null`; structured `context` contains `tenant_id` and `module_code` for log paths |

### Gate-discipline extension

`tests/integration/test_gate_discipline.py::test_gate_discipline_platform_only_writes_declare_audience` parameterised tuple list grows from the existing 4 (POST/PATCH/suspend/activate on tenants) to 6 by adding `("POST", "/api/v1/module-access/{tenant_id}/{module_code}/enable")` and `("POST", "/api/v1/module-access/{tenant_id}/{module_code}/disable")`.

### Smoke catalogue (`scripts/smoke_curl.sh`)

WHAT'S CHECKED 32 to 38. Six new entries, all under one tenant for cleanup simplicity:

1. `module_access_enable_upsert_create` , SUPER_ADMIN POST `/enable` on a `(tenant_id, NEW_MODULE)` pair where no row exists; expect 200 + `status=ENABLED`.
2. `module_access_enable_noop_on_enabled` , repeat the same call; expect 200 + row unchanged (compare `updated_at` if surfaced).
3. `module_access_disable_flip` , POST `/disable` on the row from step 1; expect 200 + `status=DISABLED`.
4. `module_access_disable_noop` , repeat the same disable call; expect 200 + row unchanged.
5. `module_access_disable_on_missing` , POST `/disable` on a `(tenant_id, OTHER_MODULE)` pair where no row exists; expect 404 `MODULE_ACCESS_NOT_FOUND`.
6. `module_access_tenant_audience_deny` , TENANT OWNER JWT on `/enable` against own tenant; expect 403 `PLATFORM_AUDIENCE_REQUIRED`.

Choose `NEW_MODULE` such that the row genuinely does not exist for the chosen tenant at smoke entry. Re-run safety: smoke leaks one DISABLED row per run (acceptable, mirrors 6.11.2's leaked-tenant pattern).

### endpoint test scripts

`scripts/test_endpoints.sh` and `scripts/test_endpoints_cloud.sh` get a Phase 4d block that mirrors smoke entries 1 through 6 across the 4 caller principals (SUPER_ADMIN, PLATFORM_ADMIN, TENANT OWNER, TENANT MEMBER) where the assertion shape differs by caller. PLATFORM_ADMIN expects 403 `PERMISSION_DENIED`; both TENANT callers expect 403 `PLATFORM_AUDIENCE_REQUIRED`.

## Verification harness

```
./scripts/check_setup.sh                            # expect 36/36
pytest -q                                            # expect ~437 passed, 0 xfailed
mypy --strict src/admin_backend tests scripts        # expect clean (73 src + tests + scripts)
./scripts/smoke_curl.sh http://localhost:8000        # expect 38/38
EXPLAIN ANALYZE on ModulesAccessRepo._transition path with seeded data;
record millisecond order-of-magnitude in the final report.
```

## Per-resource regression checkpoint

Pre-execution capture pytest counts per existing router file. Post-execution counts MUST match baselines for every file other than the four touched (`test_gate_discipline.py` +0 tests +1 tuple in existing test, plus the 4 new test files).

## Surface-and-stop scenarios

0. Any file from the pre-flight list missing. Stop.
1. `ModuleCode` Python enum is named differently or located in an unexpected module. Surface the canonical class name; do not proceed with a guessed import path.
2. `core.tenant_module_access` live-DB DDL diverges from this prompt's DDL facts (snapshot or live-DB, either way). Surface the diff; the operator decides whether the prompt or the schema needs updating before code lands. If only the snapshot is stale (3a fails but 3b passes), proceed with code lands but note the snapshot drift in the report so the operator can refresh `current_schema.sql` separately.
3. `ADMIN.TENANTS.OVERRIDE.GLOBAL` is not present in the seed `permissions` table at execution time, or SUPER_ADMIN does not hold the grant. Surface; the seed reload precondition was missed.
4. The `require()` factory does not accept `audience` as a keyword. Surface; 6.11.1 foundations did not land as expected.
5. `get_tenant_anchor` does not raise `TenantNotFoundError` on miss (e.g., returns None). Surface; the Step 6.9.3.2 invariant has regressed.
6. Repo SELECT FOR UPDATE blocks unexpectedly or the UniqueViolation retry path observes inconsistent state across retry. Surface with the failing test output.

## Report shape (Phase 5 commit-ready report)

```
Step 6.15 , DONE-LOCAL

Files changed:
  N file paths with line counts

Tests:  pytest 416 -> ~437; mypy strict clean (file count); check_setup 36/36;
        smoke_curl 38/38 local

Per-resource regression: clean. Every pre-existing router test file at baseline count.

Locked-decision honour record:
  LD1 ... LD8 , each marked as Honoured / Adjusted with one-line rationale

Surface-and-stop findings: 0 to N items, each with file:line and resolution

Design decisions made independently (none expected; surface any that arose):

Documentation:
  architecture_RBAC.md  +N lines via Appendix A
  endpoints/module-access.md  +N lines
  endpoints/openapi.json regenerated
  CLAUDE.md  +N lines (2 FN-ABs + 1 cookbook entry)
  BUILD_PLAN.md status flip + Step 6.7 wording correction

Cloud deploy: deferred per Phase 5.5 operator pause; batched verification next cycle.
```

## Coordination

- Unblocks: nothing currently blocked on this. Step 6.16 (audit-log emission) and Step 6.14 (role-assignment writes) are the next adjacent steps.
- Post-step: cloud deploy via the standard 12-step workflow. No DDL migration; no env-var change; no IAM change; `--update-env-vars` deploy ready.
- Future references: at Step 6.16, the cross-resource transition-matrix asymmetry FN-AB resurfaces; resolution feasible there once audit-log emission lands.

## Non-goals (explicit scope-out)

- No DDL changes, no Alembic migration, no seed Excel changes.
- No multi-audience writes. PLATFORM-only is locked.
- No cascade write to `tenant_user_role_assignments`. The access cascade is structural via the `has_permission()` JOIN; disabling a module blocks access on the next request without touching the assignment table. The 6.7 scope-out wording is corrected to reflect this.
- No new D-NN decision for the URL convention pattern. Codify when role-assignment writes at 6.14 give the second worked example.
- No code-volume tightening. Calibration data point captured in retro; tightening decision deferred until the next data point.

---

## Appendix A: `docs/architecture_RBAC.md` content to apply at Step 6.15

Insert this block as a new `### Worked example` subsection under `## Adding a new endpoint (cookbook)`, slotted after the existing `### Worked example: POST /tenants/{tenant_id}/suspend and /activate` block and before `### Worked example: POST /tenant-users (multi-audience create with bundled role assignments)`. Live-runtime details (exact factory signatures, import paths) may need adjustment; correct them at apply time and surface the corrections in the final report.

```
### Worked example: POST /module-access/{tenant_id}/{module_code}/enable and /disable (platform-only with upsert seam)

Two named transition endpoints sharing the same gate tuple as
tenant suspend/activate (`ADMIN.TENANTS.OVERRIDE.GLOBAL`,
SUPER_ADMIN only). Differ from tenants suspend/activate on three
axes: the enable endpoint has an upsert seam (creates a row when
no `tenant_module_access(tenant_id, module)` exists), no-op cases
return 200 idempotently (no 409 INVALID_STATE_TRANSITION), and
the resource lives under the reads' URL prefix (`/module-access/`)
rather than under the affecting parent (`/tenants/{id}/...`).

The URL convention divergence is intentional: when reads of a
resource live at a non-parent prefix, writes follow the reads so
the resource has one URL prefix end to end. When reads live at the
parent prefix (`/tenants/{id}` for tenant suspend/activate), writes
nest under the parent. The 6.7 reads at `/module-access/...` set
the precondition that drove this choice; do not extrapolate
without checking the read posture.

```python
@router.post(
    "/{tenant_id}/{module_code}/enable",
    response_model=ModuleAccessRead,
)
async def enable_module_for_tenant(
    tenant_id: UUID,
    module_code: ModuleCode,
    _: None = Depends(require(
        ModuleCode.ADMIN,
        PermissionResource.TENANTS,
        PermissionAction.OVERRIDE,
        PermissionScope.GLOBAL,
        audience="PLATFORM",
        anchor_dep=get_tenant_anchor,
    )),
    auth: AuthContext = Depends(get_auth_context),
    session: AsyncSession = Depends(get_tenant_session_dep),
) -> Any:
    """Enable module for tenant; upserts if no row exists.

    Idempotent: enable on already-ENABLED is 200 with no row
    mutation. DISABLED to ENABLED overwrites enabled_at and clears
    disabled_at atomically per the DDL CHECK pair.
    """
    row = await _repo.enable(
        session, tenant_id, module_code,
        actor_user_id=auth.user_id,
    )
    return _read_from_row(row)


@router.post(
    "/{tenant_id}/{module_code}/disable",
    response_model=ModuleAccessRead,
)
async def disable_module_for_tenant(
    tenant_id: UUID,
    module_code: ModuleCode,
    _: None = Depends(require(
        ModuleCode.ADMIN,
        PermissionResource.TENANTS,
        PermissionAction.OVERRIDE,
        PermissionScope.GLOBAL,
        audience="PLATFORM",
        anchor_dep=get_tenant_anchor,
    )),
    auth: AuthContext = Depends(get_auth_context),
    session: AsyncSession = Depends(get_tenant_session_dep),
) -> Any:
    """Disable module for tenant; 404 if no row exists.

    Idempotent: disable on already-DISABLED is 200 with no row
    mutation. ENABLED to DISABLED sets disabled_at and
    disabled_by_user_id atomically per the DDL CHECK pair;
    enabled_at carries forward as the historical record of when
    the just-ended ENABLED stint began.
    """
    row, result = await _repo.disable(
        session, tenant_id, module_code,
        actor_user_id=auth.user_id,
    )
    if result is TransitionResult.NOT_FOUND:
        raise ModuleAccessNotFoundError(
            tenant_id=tenant_id, module_code=module_code.value,
        )
    return _read_from_row(row)
```

Transition matrix:

| Current row state | enable | disable |
|---|---|---|
| missing | INSERT, 200 | 404 `MODULE_ACCESS_NOT_FOUND` |
| DISABLED | UPDATE to ENABLED (overwrites enabled_at, clears disabled_*), 200 | 200 no-op |
| ENABLED | 200 no-op | UPDATE to DISABLED (sets disabled_*, preserves enabled_at), 200 |

Access cascade is structural, not imperative. `has_permission()`'s
TENANT path JOINs `tenant_module_access` and filters
`status='ENABLED'`. A DISABLED row fails the JOIN; every
TENANT-side permission check against that module returns false on
the next request. The role assignment table is never touched by
these endpoints. Re-enable restores access automatically without
re-granting roles (D-24 per-request resolution makes this safe).

What this demonstrates: same gate tuple can drive different
matrices (suspend/activate strict-409; module-access idempotent-200)
when the resource's audit-trail and operational profile differ;
URL convention follows the reads when reads do not live under an
affecting parent; structural cascade via JOIN filter beats
imperative cascade writes for permission revocation.
```

End of Appendix A.
