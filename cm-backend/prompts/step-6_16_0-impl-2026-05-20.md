# Step 6.16.0: Audit log subsystem design document (pre-step)

## Standing discipline

- Project context docs (CLAUDE.md, BUILD_PLAN.md, docs/architecture.md, docs/architecture_RBAC.md) are pre-loaded as project knowledge.
- No code, no schema migration, no application changes. Pure documentation step.
- One commit. Report and authorise once. A6 fold-in: docs + BUILD_PLAN.md sub-step expansion + CLAUDE.md pointer all bundle into the single Phase 5 commit.
- A9 pre-flight gate: CC reports findings, operator authorises, then implementation.
- No em-dashes in any document text this step adds (CLAUDE.md standing rule).
- A8 cite-or-verify discipline: every codebase claim in the new doc references the file path or grep-verifiable evidence.

## Context

Step 6.16 (audit log writes from app) is a substantial subsystem deliverable. Design work has been completed in a Chat session over several hours covering: the routing principle (which table receives which row), the read-access principle (who sees what), the schema shape across both tables (symmetric column shape with NULL flexibility on platform table), the emission contract (synchronous, same transaction), the read contract (filters, search, cursor pagination), scale considerations (telemetry triggers and forward options), and open deferred items.

Step 6.16.0 lands this design as a dedicated subsystem design document, mirroring the existing pattern of `docs/architecture_RBAC.md` (the RBAC subsystem design doc). It also breaks Step 6.16 into 5 sub-steps in BUILD_PLAN.md.

This is a documentation-only commit. The schema, the emission code, the GET endpoint all land in subsequent sub-steps (6.16.1 through 6.16.5).

Stage at HEAD `66e79b0` (post-Step-6.20.3, DONE-LOCAL). Pytest baseline 689; check_setup 36/36; mypy strict clean on 76 source files.

## Pre-flight items (run before any change; surface-and-stop on miss)

1. Run `./scripts/check_setup.sh`. Expect 36/36 PASS.
2. Verify `docs/architecture_audit_logs.md` does NOT exist in the repo (this step creates it).
3. Verify `docs/architecture_RBAC.md` exists (this step mirrors its pattern; A8 cite-or-verify discipline requires the cross-reference to be valid).
4. Verify Step 6.16 entry in BUILD_PLAN.md is at its current TODO stub state (Status TODO, no sub-step expansion). If the entry has already been edited or sub-steps already exist, STOP and surface.
5. Verify next-available FN-AB number is 63 (FN-AB-62 was used at Step 6.20.3). If higher than 63, surface the drift.
6. Verify next-available D-NN number is 36 (D-35 was last used). If higher than 36, surface the drift.

## Scope of this commit

Single commit. Three file changes:

### NEW: `docs/architecture_audit_logs.md`

The full subsystem design document. Content provided in Appendix A. The document is 391 lines with 12 top-level sections and 0 em-dashes. It is canonical reference for the audit log subsystem; sub-step docs (6.16.1 through 6.16.5) reference back to it rather than redocumenting principles.

### MODIFY: `BUILD_PLAN.md` (replace Step 6.16 stub with sub-step expansion)

The current Step 6.16 entry is the original TODO stub ("(Detail to be elaborated when work begins.)"). Replace it with a sub-step structure following the precedent of Step 6.17 (which has 4 sub-steps) and Step 6.18 (3 sub-steps). Full replacement text in Appendix B.

The replacement preserves the existing BUILD_PLAN.md structure: Step 6.17, 6.18, 6.20.x come after, and the horizontal-rule separator between the Step 6.16 block and the Step 6.17 block remains intact.

### MODIFY: `CLAUDE.md` (add pointer in Completed section)

One-line pointer per post-485d123 / post-1cd5b8b lean convention. Position the entry in the Completed section above the existing Step 6.20.3 entry (newest at top by convention). Full pointer text in Appendix C.

## Verification

Documentation-only commit. Full harness:

```
./scripts/check_setup.sh                            # expect 36/36 (unchanged)
pytest -q                                            # expect 689 (unchanged)
mypy --strict src/admin_backend                      # expect clean (unchanged; no source code touched)
```

No DDL change, no migration, no application code, no test count delta, no smoke change.

## Surface-and-stop scenarios

1. `docs/architecture_audit_logs.md` already exists in the repo (someone else created it). STOP and surface; the design content from Appendix A may need merge with existing content.
2. Step 6.16 entry in BUILD_PLAN.md is no longer the TODO stub (someone else edited it). STOP and surface.
3. CLAUDE.md FN-AB or D-NN numbering has drifted past expected values. STOP and surface; suggests work happened that isn't in session memory.
4. `docs/architecture_RBAC.md` does not exist at the expected path. STOP and surface; the design doc references this file as the pattern precedent and the cross-reference would be broken.

## Report shape (Phase 5 commit-ready)

Per A7 commit-hash gate. Report includes:

- Files changed list with line counts.
- Confirmation that pytest, check_setup, mypy strict counts are unchanged.
- Documentation note: design doc landed at `docs/architecture_audit_logs.md`; BUILD_PLAN.md Step 6.16 expanded to sub-step structure; CLAUDE.md 1-line pointer added.
- Surface-and-stop findings: 0 to N items.
- Cloud deploy: not needed for docs-only commit.
- Commit hash for operator authorisation.

## Coordination

- Unblocks: Step 6.16.1 (schema implementation).
- No frontend coordination required (docs are internal reference; frontend has been informed about the audit subsystem plan in operator conversation).
- Cloud SQL: no changes.
- No cloud deploy.

## Non-goals (explicit scope-out)

- No schema changes; no Alembic migration.
- No application code; no router, no repo, no emission helper, no GET endpoint.
- No catalogue/seed changes.
- No tests added or modified.
- No architecture_RBAC.md changes (the new doc is a sibling, not an extension).
- No architecture.md changes (audit subsystem has its own design doc, not part of the top-level architecture overview).

---

## Appendix A: content for `docs/architecture_audit_logs.md`

The file content is provided as a separate attachment (`architecture_audit_logs.md`, 391 lines, 0 em-dashes). CC drops the attached file at `docs/architecture_audit_logs.md` verbatim. No transformation; no edits.

The file structure is for orientation only (CC does not need to reproduce this; the attached file is the source of truth):

- 12 top-level sections: Overview, Routing principle, Read-access principle, Architecture, Schema, Emission contract, Read contract, Scale considerations, Open questions and deferred decisions, Sub-step plan, Related documents.
- Schema section covers both tables column-by-column, the symmetric column shape rationale, enums, denormalised label rationale.
- Emission contract section covers synchronous semantics, what gets logged, row granularity, per-result_type payload shapes, emission failure handling.

## Appendix B: replacement text for BUILD_PLAN.md Step 6.16 section

Replace the existing Step 6.16 block (currently the TODO stub ending in "(Detail to be elaborated when work begins.)") with the following text. Preserve the horizontal-rule separator between the new Step 6.16 block and the existing Step 6.17 block.

Begin replacement:

```
## Step 6.16 - Audit log subsystem

**Status.** IN PROGRESS (design landed at 6.16.0; sub-steps 6.16.1 through 6.16.5 implement schema, emission, and read endpoint).

**Owner.** CLAUDE_CODE (impl across sub-steps) + HUMAN (catalogue Excel + Cloud SQL UPSERT at 6.16.3).

**Blocked by.** None.

**Goal.** Ship the audit log subsystem: two physically separate tables (tenant + platform) with symmetric column shape and mixed RLS posture, synchronous emission from all v0 write endpoints (12 endpoints across 4 resource families), GET endpoint backing the frontend audit timeline, and permission tuples gating audit-log access. Architectural pattern mirrors Step 6.8.1's split of user_role_assignments per D-29.

**Design reference.** Full subsystem design captured at `docs/architecture_audit_logs.md` (landed at Step 6.16.0). All sub-steps implement against this design document; the design doc is the authoritative reference.

**Sub-steps.** 6.16.0 (design doc + sub-step plan) -> 6.16.1 (schema) -> 6.16.2 (tenants emission) -> 6.16.3 (GET endpoint + permission catalogue) -> 6.16.4 (tenant-users + role-assignments emission) -> 6.16.5 (module-access + org-tree emission).

------

### Step 6.16.0 - Audit log subsystem design document (pre-step)

**Status.** DONE-LOCAL (this commit).
**Owner.** CHAT (design) + CLAUDE_CODE (commit shape).
**Blocks.** 6.16.1.

**Goal.** Land the subsystem design document at `docs/architecture_audit_logs.md` and the sub-step plan in BUILD_PLAN.md. No code, no schema, no test changes.

**Scope in.**

- `docs/architecture_audit_logs.md` NEW: 391-line design document covering purpose, routing principle, read-access principle, schema (symmetric two-table design), emission contract, read contract, scale considerations, open deferred items, sub-step plan.
- `BUILD_PLAN.md`: Step 6.16 entry expanded from TODO stub to sub-step structure with 6 sub-step blocks (6.16.0 through 6.16.5).
- `CLAUDE.md`: 1-line pointer to the new design doc in the Completed section per post-485d123 lean convention.

**Scope out.** No schema, no application code, no tests.

**Acceptance.** Design document lands; BUILD_PLAN.md reflects sub-step plan; check_setup remains 36/36; pytest baseline unchanged at 689.

**Coordination.** No cloud deploy needed (docs-only). Frontend can read the new design document for context on the audit subsystem.

**Rough effort.** ~30 minutes.

------

### Step 6.16.1 - Audit log schema (DDL + ORM + RLS + indexes)

**Status.** TODO.
**Owner.** CLAUDE_CODE.
**Blocked by.** 6.16.0.

**Goal.** Land the database schema for both audit log tables per the design at `docs/architecture_audit_logs.md`. Schema only: 2 tables, 1 new enum, RLS posture, indexes, ORM models, schema tests. No application code, no emission.

**Scope in.**

- New Alembic migration creating: `core.audit_result_type_enum` (6 values), `core.tenant_activity_audit_logs` table (16 columns + FK + 2 CHECK constraints + RLS+FORCE+D-29 OR-branch + 3 indexes), `core.platform_activity_audit_logs` table (same 16 columns with tenant_id/tenant_name NULLABLE + FK + 2 CHECK constraints + 2 indexes; no RLS).
- ORM models: `TenantActivityAuditLog`, `PlatformActivityAuditLog` in `src/admin_backend/models/audit_log.py`. Python `AuditResultType` enum mirroring the SQL enum. Reuse existing `ActorUserType` enum for `actor_user_type` column.
- Tests: ~12-15 new tests covering migration roundtrip, ORM persistence, NULL-pair CHECK constraints, RLS posture, FK behavior.
- Regenerate `docs/schema/current_schema.sql`.

**Scope out.** No application code; no emission; no GET endpoint; no permission catalogue change.

**Acceptance.** Migration applies and downgrades cleanly. Schema matches design doc. RLS policy enforces D-29 OR-branch on tenant table. Both tables UNION-compatible at the column level. Tests green. mypy strict clean. check_setup 36/36.

**Coordination.** No cloud deploy at sub-step end; batched with subsequent sub-steps at next Phase 6 cycle.

**Rough effort.** ~3 hours.

------

### Step 6.16.2 - Audit emission: tenants endpoints

**Status.** TODO.
**Owner.** CLAUDE_CODE.
**Blocked by.** 6.16.1.

**Goal.** Wire synchronous audit emission into 4 write endpoints on `/tenants`: POST (create), PATCH, suspend, activate. All emission inside the same transaction as the data write per design doc emission contract.

**Scope in.**

- Shared audit-emission helper in a new module under `src/admin_backend/audit/`. Helper takes auth context, action, resource details, result_type, details payload; routes to correct table per routing principle.
- Updates to `TenantsRepo.create`, `update`, `transition` methods to call the audit helper.
- Tests: emission unit tests + integration tests verifying audit rows produced on each endpoint outcome (success + failure paths per `result_type` taxonomy).
- The tenant-creation exception: success row goes to platform_activity_audit_logs (per routing principle).

**Scope out.** Other endpoints (later sub-steps); GET endpoint (6.16.3).

**Acceptance.** All 4 tenants endpoints emit audit rows on success and failure. Atomicity verified (rollback on emission failure rolls back data write). Tests green.

**Coordination.** Batched cloud deploy.

**Rough effort.** ~3-4 hours.

------

### Step 6.16.3 - GET endpoint + permission catalogue

**Status.** TODO.
**Owner.** CLAUDE_CODE (endpoint) + HUMAN (Excel + Cloud SQL UPSERT).
**Blocked by.** 6.16.2.

**Goal.** Ship the GET endpoint backing the frontend audit timeline. Filters, search, cursor pagination per the design doc. Permission tuples added to catalogue (Excel + local + Cloud SQL in lockstep, matching the Step 6.13 / 6.17.1 / 6.18.1 precedent).

**Scope in.**

- Permission catalogue addition: 2 new permission rows for tenant + platform audit view, role grants for SUPER_ADMIN / PLATFORM_ADMIN / OWNER per the read-access principle. Excel + local DB + Cloud SQL UPSERT in the standard catalogue-update workflow.
- New router endpoint(s) at `/api/v1/audit/activities` (final URL TBD at impl design). UNION ALL across both tables for platform users; tenant-table-only for tenant users (RLS enforces).
- Cursor-paginated, accepts `from` / `to` / `status` / `tenant_id` / `scope` filters and `search` parameter per design doc.
- Tests covering filter combinations, RLS scoping, cursor stability, search semantics.

**Scope out.** Other emission endpoints (later sub-steps).

**Acceptance.** Frontend can query the audit log via the GET endpoint. Filtering and pagination work end-to-end. Tenant scoping verified via RLS. Tests green.

**Coordination.** Cloud SQL catalogue update applied in lockstep with local. Frontend integrates within 24 hours of deploy. Per the operator-communicated plan: audit log will show only tenants-endpoint events until 6.16.4 and 6.16.5 ship the remaining emission.

**Rough effort.** ~4 hours.

------

### Step 6.16.4 - Audit emission: tenant-users + role-assignments endpoints

**Status.** TODO.
**Owner.** CLAUDE_CODE.
**Blocked by.** 6.16.3.

**Goal.** Wire audit emission into 4 endpoints on `/tenant-users` (POST, PATCH, suspend, activate). PATCH includes the diff-replace on `roles[]` which produces a single audit row per HTTP request per design doc row-granularity decision.

**Scope in.**

- Updates to `TenantUsersRepo.create`, `update`, `transition` methods to call the audit helper.
- Tests covering all 4 endpoints across success and failure paths.

**Scope out.** Other endpoints (next sub-step).

**Acceptance.** Tests green. Audit rows produced match design doc payload shapes per result_type.

**Coordination.** Batched cloud deploy.

**Rough effort.** ~2-3 hours.

------

### Step 6.16.5 - Audit emission: module-access + org-tree endpoints

**Status.** TODO.
**Owner.** CLAUDE_CODE.
**Blocked by.** 6.16.4.

**Goal.** Wire audit emission into remaining 4 write endpoints: module-access enable/disable + org-tree POST add-node + org-tree PATCH edit-node.

**Scope in.**

- Updates to `ModulesAccessRepo` and `OrgNodesRepo` methods to call the audit helper.
- Tests covering all 4 endpoints.

**Scope out.** None (this closes the 6.16 series).

**Acceptance.** All 12 v0 write endpoints emit audit rows. Tests green.

**Coordination.** Cloud deploy. Step 6.16 closes; frontend has full audit-log coverage.

**Rough effort.** ~2-3 hours.

------
```

End replacement.

## Appendix C: CLAUDE.md pointer text

Add the following entry to the CLAUDE.md Completed section, positioned above the existing Step 6.20.3 entry (newest at top by convention):

```
- **Step 6.16.0: Audit log subsystem design document.** DONE-LOCAL 2026-05-MM. Landed `docs/architecture_audit_logs.md` capturing the full subsystem design (routing principle, read-access principle, symmetric two-table schema, sync emission, read contract, scale considerations, deferred items) plus BUILD_PLAN.md sub-step expansion (6.16.0 through 6.16.5). No code or schema changes. Detail: `docs/architecture_audit_logs.md`.
```

Replace `2026-05-MM` with the actual commit date.
