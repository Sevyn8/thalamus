// Placeholder. `pnpm gen:dis-types` overwrites this file with generated
// types when `docs/dis-openapi.json` is present (Sanjeev ships the spec
// in Phase 5d). Until then, the script no-ops with a stderr warning
// and this file stays empty — a valid TS module so future imports
// don't fail to resolve at build time.
export {};
